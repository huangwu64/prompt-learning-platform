# 零基础学AI平台 - 接口文档

| 项目名称 | 零基础学AI平台 |
|---------|--------------|
| 文档版本 | v2.0 |
| 更新日期 | 2026-08-11 |
| Base URL | `http://localhost:8080`（开发环境） |
| API 前缀 | `/api` |
| 技术栈 | Java 21 + Spring Boot 3 + MyBatis-Plus + MySQL 8.0 + Redis 7 |

---

## 一、通用规范

### 1.1 请求规范

- 所有接口统一前缀：`/api`
- 请求体格式：`application/json`
- 字符编码：UTF-8
- 认证方式：请求头携带 `Authorization: Bearer <JWT_TOKEN>`

### 1.2 统一响应格式

所有接口返回统一的 JSON 结构：

```json
{
  "success": true,
  "data": {},
  "error": null
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| success | boolean | 请求是否成功 |
| data | any / null | 成功时返回的业务数据，失败时为 null |
| error | string / null | 失败时的错误描述，成功时为 null |

**成功响应示例：**

```json
{
  "success": true,
  "data": {
    "id": "conv_abc123",
    "originalPrompt": "帮我写个邮件"
  },
  "error": null
}
```

**失败响应示例：**

```json
{
  "success": false,
  "data": null,
  "error": "邮箱或密码错误"
}
```

### 1.3 认证机制

| 接口类型 | 是否需要认证 | 说明 |
|---------|-------------|------|
| 注册 `POST /api/auth/register` | 否 | — |
| 登录 `POST /api/auth/login` | 否 | — |
| 健康检查 `GET /api/health` | 否 | — |
| 其他所有接口 | 是 | 请求头需携带 `Authorization: Bearer <token>` |

**认证流程：**

```
1. 用户登录 → POST /api/auth/login → 返回 JWT token
2. 前端存储 token 到 localStorage
3. 后续请求携带 Header: Authorization: Bearer <token>
4. 后端 auth 中间件校验 token
5. token 有效 → 放行；token 无效/过期 → 返回 401
6. token 有效期：7 天
```

### 1.4 错误码

| HTTP 状态码 | 含义 | 触发场景 |
|------------|------|---------|
| 200 | 请求成功 | 正常业务响应 |
| 400 | 参数错误 | 请求体格式不合法、必填字段缺失、字段校验失败 |
| 401 | 未认证 | 未携带 token、token 无效或已过期 |
| 403 | 无权限 | 已认证但无权操作该资源（如操作他人数据） |
| 404 | 资源不存在 | 请求的对话、作品等不存在 |
| 429 | 请求过于频繁 | 触发限流（单 IP 每分钟 > 60 次） |
| 500 | 服务器错误 | 后端异常、数据库错误、AI 服务异常 |

### 1.5 分页规范

列表类接口统一使用分页参数：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| page | integer | 1 | 页码，从 1 开始 |
| pageSize | integer | 20 | 每页条数，最大 100 |

分页响应结构：

```json
{
  "success": true,
  "data": {
    "items": [],
    "total": 100,
    "page": 1,
    "pageSize": 20,
    "totalPages": 5
  }
}
```

### 1.6 ID 规范

- 用户 ID：`user_` 前缀 + UUID，如 `user_a1b2c3d4`
- 对话 ID：`conv_` 前缀 + UUID，如 `conv_abc123`
- 消息 ID：`msg_` 前缀 + UUID，如 `msg_def456`
- 作品 ID：`work_` 前缀 + UUID，如 `work_ghi789`

---

## 二、认证模块

### 2.1 用户注册

注册新用户账号。

```
POST /api/auth/register
```

**认证：** 否

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 校验规则 |
|------|------|------|---------|---------|
| email | string | 是 | 5—100 字符 | 合法邮箱格式 |
| password | string | 是 | 6—50 字符 | 至少 6 位 |
| username | string | 是 | 2—30 字符 | 唯一，不支持特殊字符 |

**请求示例：**

```json
{
  "email": "user@example.com",
  "password": "myPassword123",
  "username": "张三"
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "user_a1b2c3d4",
      "email": "user@example.com",
      "username": "张三",
      "streakDays": 0,
      "createdAt": "2026-08-11T10:00:00.000Z"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "邮箱格式不合法" | email 不符合邮箱格式 |
| 400 | "密码至少 6 位" | password 少于 6 位 |
| 400 | "用户名长度需在 2—30 字符之间" | username 长度不合规 |
| 400 | "该邮箱已注册" | email 已存在 |
| 400 | "该用户名已被使用" | username 已存在 |

---

### 2.2 用户登录

用户登录并获取 JWT token。

```
POST /api/auth/login
```

**认证：** 否

**请求体：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| email | string | 是 | 注册时的邮箱 |
| password | string | 是 | 注册时的密码 |

**请求示例：**

```json
{
  "email": "test@example.com",
  "password": "123456"
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "user": {
      "id": "user_test001",
      "email": "test@example.com",
      "username": "测试用户",
      "streakDays": 7,
      "createdAt": "2026-07-01T08:00:00.000Z"
    },
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "邮箱格式不合法" | email 不符合邮箱格式 |
| 400 | "邮箱或密码错误" | email 不存在或 password 不匹配 |
| 429 | "登录失败次数过多，请 15 分钟后再试" | 连续失败 5 次 |

---

## 三、苏格拉底对话模块

### 3.1 创建对话

用户输入初始提示词，创建一个新的苏格拉底对话，AI 返回第一轮追问。

```
POST /api/chat
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 说明 |
|------|------|------|---------|------|
| originalPrompt | string | 是 | 1—500 字符 | 用户的初始提示词 |
| topicId | string | 否 | — | 关联的学习地图知识点 ID（从学习地图跳转时携带） |

**请求示例：**

```json
{
  "originalPrompt": "帮我写个邮件",
  "topicId": "topic_role_setting"
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "conversation": {
      "id": "conv_abc123",
      "userId": "user_test001",
      "originalPrompt": "帮我写个邮件",
      "improvedPrompt": null,
      "comparisonResult": null,
      "rating": null,
      "status": "active",
      "currentRound": 1,
      "maxRounds": 5,
      "createdAt": "2026-08-11T10:00:00.000Z"
    },
    "message": {
      "id": "msg_001",
      "role": "assistant",
      "content": "这封邮件的收件人是谁？你希望传达什么情绪？",
      "messageType": "question",
      "createdAt": "2026-08-11T10:00:01.000Z"
    }
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "提示词不能为空" | originalPrompt 为空或纯空格 |
| 400 | "提示词长度不能超过 500 字符" | originalPrompt 超长 |
| 400 | "你有一个进行中的对话，请先完成" | 同一用户已有 active 状态对话 |
| 500 | "AI 服务暂时不可用，请稍后重试" | DeepSeek API 调用失败 |

---

### 3.2 获取对话历史列表

获取当前用户的对话历史列表，按创建时间倒序。

```
GET /api/chat/history
```

**认证：** 是

**查询参数：**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 20 | 每页条数 |
| status | string | 否 | — | 按状态筛选：active / completed |

**请求示例：**

```
GET /api/chat/history?page=1&pageSize=10&status=completed
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "conv_abc123",
        "originalPrompt": "帮我写个邮件",
        "improvedPrompt": "你是一位资深HR...",
        "rating": 4,
        "status": "completed",
        "currentRound": 3,
        "createdAt": "2026-08-11T10:00:00.000Z",
        "updatedAt": "2026-08-11T10:05:00.000Z"
      }
    ],
    "total": 45,
    "page": 1,
    "pageSize": 10,
    "totalPages": 5
  },
  "error": null
}
```

---

### 3.3 获取对话详情

获取单个对话的完整信息，包含所有消息记录。

```
GET /api/chat/:id
```

**认证：** 是

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 对话 ID |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "conversation": {
      "id": "conv_abc123",
      "userId": "user_test001",
      "originalPrompt": "帮我写个邮件",
      "improvedPrompt": "你是一位资深HR，请帮我撰写一封正式的商务邮件...",
      "comparisonResult": {
        "improvements": [
          "增加了角色设定：资深HR",
          "补充了目标受众信息",
          "结构化了输出格式要求",
          "明确了语气约束：正式、专业"
        ]
      },
      "rating": 4,
      "status": "completed",
      "currentRound": 3,
      "maxRounds": 5,
      "createdAt": "2026-08-11T10:00:00.000Z",
      "updatedAt": "2026-08-11T10:05:00.000Z"
    },
    "messages": [
      {
        "id": "msg_001",
        "role": "assistant",
        "content": "这封邮件的收件人是谁？你希望传达什么情绪？",
        "messageType": "question",
        "createdAt": "2026-08-11T10:00:01.000Z"
      },
      {
        "id": "msg_002",
        "role": "user",
        "content": "收件人是我的直属上级，希望传达正式且尊重的语气",
        "messageType": "answer",
        "createdAt": "2026-08-11T10:00:30.000Z"
      },
      {
        "id": "msg_003",
        "role": "assistant",
        "content": "这封邮件的主要目的是什么？是汇报工作、请示审批还是其他？",
        "messageType": "question",
        "createdAt": "2026-08-11T10:00:35.000Z"
      }
    ]
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "对话不存在" | id 无效 |
| 403 | "无权访问该对话" | 对话不属于当前用户 |

---

### 3.4 用户回复消息

用户回复 AI 的追问，AI 返回下一轮追问或判断信息充足后结束对话。

```
POST /api/chat/:id/message
```

**认证：** 是

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 对话 ID |

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 说明 |
|------|------|------|---------|------|
| content | string | 是 | 1—1000 字符 | 用户的回复内容 |

**请求示例：**

```json
{
  "content": "收件人是我的直属上级，希望传达正式且尊重的语气"
}
```

**成功响应 — AI 继续追问（200）：**

```json
{
  "success": true,
  "data": {
    "message": {
      "id": "msg_003",
      "role": "assistant",
      "content": "这封邮件的主要目的是什么？是汇报工作、请示审批还是其他？",
      "messageType": "question",
      "createdAt": "2026-08-11T10:00:35.000Z"
    },
    "conversationStatus": "active",
    "currentRound": 2,
    "maxRounds": 5
  },
  "error": null
}
```

**成功响应 — AI 判断信息充足，自动完成（200）：**

```json
{
  "success": true,
  "data": {
    "message": {
      "id": "msg_005",
      "role": "assistant",
      "content": "我已经收集了足够的信息，下面是优化后的提示词...",
      "messageType": "summary",
      "createdAt": "2026-08-11T10:03:00.000Z"
    },
    "conversationStatus": "completed",
    "currentRound": 3,
    "maxRounds": 5,
    "improvedPrompt": "你是一位资深HR，请帮我撰写一封正式的商务邮件...",
    "comparisonResult": {
      "improvements": [
        "增加了角色设定：资深HR",
        "补充了目标受众信息",
        "结构化了输出格式要求"
      ]
    }
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "回复内容不能为空" | content 为空 |
| 400 | "回复内容不能超过 1000 字符" | content 超长 |
| 404 | "对话不存在" | id 无效 |
| 400 | "该对话已结束，无法继续回复" | 对话状态为 completed |
| 500 | "AI 服务暂时不可用，请稍后重试" | DeepSeek API 调用失败 |

---

### 3.5 完成对话

用户主动结束对话，强制 AI 生成优化结果。用于用户不想继续回答追问、直接查看结果的场景。

```
POST /api/chat/:id/complete
```

**认证：** 是

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 对话 ID |

**请求体：** 无需请求体

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "conversation": {
      "id": "conv_abc123",
      "status": "completed",
      "originalPrompt": "帮我写个邮件",
      "improvedPrompt": "你是一位资深HR，请帮我撰写一封正式的商务邮件。收件人是我的直属上级，邮件目的是汇报本季度项目进展。请使用正式、尊重的语气，输出格式包含：邮件主题、称呼、正文（含项目概况、关键成果、下季度计划）、落款。",
      "comparisonResult": {
        "improvements": [
          "增加了角色设定：资深HR",
          "补充了目标受众：直属上级",
          "明确了邮件目的：汇报项目进展",
          "结构化了输出格式：主题+称呼+正文+落款",
          "约束了语气：正式、尊重"
        ]
      }
    }
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "对话不存在" | id 无效 |
| 400 | "该对话已完成" | 对话状态已是 completed |
| 500 | "AI 服务暂时不可用，请稍后重试" | DeepSeek API 调用失败 |

---

### 3.6 对话评分

用户对本次对话的学习体验进行评分。评分将同步到学习地图的进度计算。

```
POST /api/chat/:id/rating
```

**认证：** 是

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 对话 ID |

**请求体：**

| 字段 | 类型 | 必填 | 范围 | 说明 |
|------|------|------|------|------|
| rating | integer | 是 | 1—5 | 1=很差，2=较差，3=一般，4=好，5=很好 |

**请求示例：**

```json
{
  "rating": 4
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "conversationId": "conv_abc123",
    "rating": 4,
    "averageRating": 4.2,
    "masteredTopics": 12
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "评分必须在 1—5 之间" | rating 超出范围 |
| 404 | "对话不存在" | id 无效 |
| 400 | "该对话尚未完成，无法评分" | 对话状态为 active |

---

## 四、提示词实验室模块

### 4.1 测试提示词

测试单条提示词，调用 DeepSeek 返回 AI 回复。用于积木构建器的"测试"按钮和实验室的单模型测试。

```
POST /api/lab/test
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 默认值 | 说明 |
|------|------|------|---------|--------|------|
| prompt | string | 是 | 1—4000 字符 | — | 要测试的提示词 |
| temperature | float | 否 | 0.0—2.0 | 0.7 | 温度参数 |
| maxTokens | integer | 否 | 1—8192 | 2048 | 最大输出 token 数 |

**请求示例：**

```json
{
  "prompt": "你是一位资深编辑，请帮我撰写一篇关于 AI 的科普文章，目标读者是初学者，输出格式为 Markdown 表格。",
  "temperature": 0.7,
  "maxTokens": 2048
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "result": "## AI 入门指南\n\n| 概念 | 说明 |\n|------|------|\n| 机器学习 | ... |",
    "model": "deepseek-chat",
    "usage": {
      "promptTokens": 45,
      "completionTokens": 320,
      "totalTokens": 365
    },
    "duration": 2.3
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "提示词不能为空" | prompt 为空 |
| 400 | "提示词长度不能超过 4000 字符" | prompt 超长 |
| 400 | "温度值必须在 0.0—2.0 之间" | temperature 超出范围 |
| 400 | "maxTokens 必须在 1—8192 之间" | maxTokens 超出范围 |
| 429 | "今日 AI 调用次数已达上限" | 超过每日 50 次限制 |
| 500 | "AI 服务暂时不可用，请稍后重试" | DeepSeek API 调用失败 |

---

### 4.2 多模型对比（待开发）

同时调用多个大语言模型对同一提示词生成回复，并排返回结果。

```
POST /api/lab/compare
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 默认值 | 说明 |
|------|------|------|---------|--------|------|
| prompt | string | 是 | 1—4000 字符 | — | 要测试的提示词 |
| models | string[] | 是 | 1—3 个 | — | 模型列表：deepseek / gpt-4o / claude-3.5 |
| temperature | float | 否 | 0.0—2.0 | 0.7 | 温度参数（对所有模型统一） |
| maxTokens | integer | 否 | 1—8192 | 2048 | 最大输出 token 数 |

**请求示例：**

```json
{
  "prompt": "用三句话解释什么是提示词工程",
  "models": ["deepseek", "gpt-4o"],
  "temperature": 0.7,
  "maxTokens": 1024
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "experimentId": "exp_xyz789",
    "results": [
      {
        "model": "deepseek",
        "status": "success",
        "result": "提示词工程是指...",
        "usage": {
          "promptTokens": 15,
          "completionTokens": 80,
          "totalTokens": 95
        },
        "duration": 1.8
      },
      {
        "model": "gpt-4o",
        "status": "success",
        "result": "Prompt engineering is...",
        "usage": {
          "promptTokens": 15,
          "completionTokens": 75,
          "totalTokens": 90
        },
        "duration": 2.1
      }
    ]
  },
  "error": null
}
```

**部分失败响应（200）：**

```json
{
  "success": true,
  "data": {
    "experimentId": "exp_xyz789",
    "results": [
      {
        "model": "deepseek",
        "status": "success",
        "result": "提示词工程是指...",
        "duration": 1.8
      },
      {
        "model": "gpt-4o",
        "status": "failed",
        "error": "API 不可用",
        "duration": 30.0
      }
    ]
  },
  "error": null
}
```

> **限流说明**：实验室实验接口（`test` / `compare`）除全局「单 IP 每分钟 60 次」限流外，额外限制为**同一用户每分钟最多 3 次实验**，超出返回 `429`。

---

## 五、健康检查

### 5.1 服务健康检查

检查后端服务和数据库连接状态。

```
GET /api/health
```

**认证：** 否

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "status": "ok",
    "timestamp": "2026-08-11T10:00:00.000Z",
    "services": {
      "database": "connected",
      "deepseek": "available"
    },
    "version": "2.0.0"
  },
  "error": null
}
```

---

## 六、学习路径模块（待开发）

### 6.1 获取学习进度

获取当前用户的学习地图进度，包括统计卡片数据、四阶段技能树状态、知识点掌握情况。

```
GET /api/learning/progress
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "stats": {
      "averageRating": 4.2,
      "streakDays": 7,
      "masteredCount": 12,
      "totalConversations": 45
    },
    "currentStage": "intermediate",
    "stages": [
      {
        "id": "beginner",
        "name": "入门",
        "status": "completed",
        "knowledgePoints": [
          {
            "id": "topic_role",
            "name": "角色设定",
            "status": "mastered",
            "bestRating": 5
          },
          {
            "id": "topic_task",
            "name": "任务描述",
            "status": "mastered",
            "bestRating": 4
          }
        ]
      },
      {
        "id": "intermediate",
        "name": "进阶",
        "status": "in_progress",
        "knowledgePoints": [
          {
            "id": "topic_cot",
            "name": "思维链",
            "status": "learning",
            "bestRating": 3
          },
          {
            "id": "topic_multi_turn",
            "name": "多轮对话",
            "status": "locked",
            "bestRating": null
          }
        ]
      }
    ]
  },
  "error": null
}
```

---

### 6.2 更新学习进度

内部接口，由对话评分等操作触发，更新知识点的掌握状态。通常不需要前端主动调用。

```
POST /api/learning/progress
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| topicId | string | 是 | 知识点 ID |
| rating | integer | 是 | 本次评分 1—5 |
| conversationId | string | 否 | 关联的对话 ID |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "topicId": "topic_role",
    "status": "mastered",
    "bestRating": 5,
    "stageUnlocked": false
  },
  "error": null
}
```

> **触发链路说明**：`POST /api/learning/progress` 为内部接口，由「对话评分」等业务动作在 Service 层自动调用，前端无需直接请求。
>
> **知识点配置说明**：学习地图的四阶段知识点（角色设定、任务描述等）存放于配置表，由数据库 Seed 脚本初始化，不提供对外管理接口。

---

## 七、作品工厂模块（待开发）

### 7.1 获取作品列表

获取当前用户的作品列表。

```
GET /api/works
```

**认证：** 是

**查询参数：**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 20 | 每页条数 |
| workType | string | 否 | — | 按类型筛选：ppt / report / email / social |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "work_ghi789",
        "workType": "email",
        "title": "季度汇报邮件",
        "content": "尊敬的张总：\n\n关于本季度项目进展...",
        "shareUrl": null,
        "createdAt": "2026-08-11T10:00:00.000Z",
        "updatedAt": "2026-08-11T10:05:00.000Z"
      }
    ],
    "total": 15,
    "page": 1,
    "pageSize": 20,
    "totalPages": 1
  },
  "error": null
}
```

---

### 7.2 生成作品

选择模板并填写表单信息，调用 DeepSeek 生成内容并保存。

```
POST /api/works
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 说明 |
|------|------|------|---------|------|
| workType | string | 是 | — | 模板类型：ppt / report / email / social |
| title | string | 是 | 1—100 字符 | 作品标题 |
| formData | object | 是 | — | 表单数据，结构根据 workType 动态变化 |

**formData 结构（按 workType）：**

| workType | formData 字段 |
|----------|--------------|
| ppt | topic(主题), audience(受众), duration(时长), keyPoints(核心要点[]) |
| report | title(标题), keyPoints(要点[]), style(风格), wordCount(字数) |
| email | recipient(收件人), purpose(目的), tone(语气), attachment(附件说明) |
| social | platform(平台), product(产品), style(风格), goal(目标) |

**请求示例（email）：**

```json
{
  "workType": "email",
  "title": "季度汇报邮件",
  "formData": {
    "recipient": "直属上级张总",
    "purpose": "汇报本季度项目进展",
    "tone": "正式",
    "attachment": "项目数据表.xlsx"
  }
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "work_ghi789",
    "workType": "email",
    "title": "季度汇报邮件",
    "content": "尊敬的张总：\n\n关于本季度项目进展，现汇报如下...\n\n一、项目概况\n...\n\n二、关键成果\n...\n\n三、下季度计划\n...\n\n此致\n敬礼\n\nXXX",
    "shareUrl": null,
    "createdAt": "2026-08-11T10:00:00.000Z"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "作品类型不合法" | workType 不在枚举内 |
| 400 | "标题不能为空" | title 为空 |
| 400 | "表单数据不完整" | formData 必填字段缺失 |
| 429 | "今日 AI 调用次数已达上限" | 超过每日限制 |
| 500 | "AI 生成失败，请重试" | DeepSeek API 调用失败 |

---

### 7.3 获取作品详情

```
GET /api/works/:id
```

**认证：** 是

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 作品 ID |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "work_ghi789",
    "userId": "user_test001",
    "workType": "email",
    "title": "季度汇报邮件",
    "content": "尊敬的张总：\n\n...",
    "formData": {
      "recipient": "直属上级张总",
      "purpose": "汇报本季度项目进展",
      "tone": "正式",
      "attachment": "项目数据表.xlsx"
    },
    "shareUrl": null,
    "createdAt": "2026-08-11T10:00:00.000Z",
    "updatedAt": "2026-08-11T10:00:00.000Z"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "作品不存在" | id 无效 |
| 403 | "无权访问该作品" | 作品不属于当前用户 |

---

### 7.4 删除作品

```
DELETE /api/works/:id
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "work_ghi789",
    "deleted": true
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "作品不存在" | id 无效 |
| 403 | "无权删除该作品" | 作品不属于当前用户 |

---

### 7.5 生成作品分享链接

将作品生成公开分享链接，生成后可凭分享码免登录访问。

```
POST /api/works/:id/share
```

**认证：** 是

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 作品 ID |

**请求体：** 无需请求体

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "work_ghi789",
    "shareCode": "a3Kp9q",
    "shareUrl": "http://localhost:8080/api/share/a3Kp9q"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "作品不存在" | id 无效 |
| 403 | "无权分享该作品" | 作品不属于当前用户 |

---

### 7.6 取消作品分享

```
DELETE /api/works/:id/share
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "work_ghi789",
    "shareCode": null,
    "shareUrl": null
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "作品不存在" | id 无效 |
| 403 | "无权取消分享" | 作品不属于当前用户 |

---

### 7.7 公开访问分享作品

免登录查看分享作品，仅返回展示所需字段，不返回任何用户隐私信息。

```
GET /api/share/:shareCode
```

**认证：** 否

**路径参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| shareCode | string | 分享码 |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "work": {
      "id": "work_ghi789",
      "workType": "email",
      "title": "季度汇报邮件",
      "content": "尊敬的张总：\n\n...",
      "author": "测试用户",
      "createdAt": "2026-08-11T10:00:00.000Z"
    }
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "分享链接不存在或已取消" | shareCode 无效或已取消分享 |

---


## 八、智能对比模块（待开发）

### 8.1 创建智能对比

用户提交两段提示词（原始和待对比），AI 分析两者的差异并给出改进建议。

```
POST /api/compare
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 说明 |
|------|------|------|---------|------|
| originalPrompt | string | 是 | 1—2000 字符 | 原始提示词 |
| comparedPrompt | string | 是 | 1—2000 字符 | 待对比的提示词 |

**请求示例：**

```json
{
  "originalPrompt": "帮我写个邮件",
  "comparedPrompt": "你是一位资深HR，请帮我撰写一封正式的商务邮件，收件人是直属上级，目的是汇报项目进展，输出格式包含主题、称呼、正文和落款。"
}
```

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "cmp_pqr678",
    "originalPrompt": "帮我写个邮件",
    "comparedPrompt": "你是一位资深HR，请帮我撰写...",
    "analysis": {
      "improvements": [
        {
          "dimension": "角色设定",
          "original": "缺失",
          "improved": "资深HR",
          "impact": "high"
        },
        {
          "dimension": "目标受众",
          "original": "缺失",
          "improved": "直属上级",
          "impact": "high"
        },
        {
          "dimension": "输出格式",
          "original": "缺失",
          "improved": "主题+称呼+正文+落款",
          "impact": "medium"
        }
      ],
      "scoreComparison": {
        "originalScore": 35,
        "comparedScore": 88,
        "improvement": 53
      }
    },
    "createdAt": "2026-08-11T10:00:00.000Z"
  },
  "error": null
}
```

---

### 8.2 获取对比历史

```
GET /api/compare/history
```

**认证：** 是

**查询参数：**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 20 | 每页条数 |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "cmp_pqr678",
        "originalPrompt": "帮我写个邮件",
        "comparedPrompt": "你是一位资深HR...",
        "scoreComparison": {
          "originalScore": 35,
          "comparedScore": 88,
          "improvement": 53
        },
        "createdAt": "2026-08-11T10:00:00.000Z"
      }
    ],
    "total": 5,
    "page": 1,
    "pageSize": 20,
    "totalPages": 1
  },
  "error": null
}
```

---

## 九、积木构建器模板模块

### 9.1 获取模板列表

```
GET /api/templates
```

**认证：** 是

**查询参数：**

| 参数 | 类型 | 必填 | 默认值 | 说明 |
|------|------|------|--------|------|
| page | integer | 否 | 1 | 页码 |
| pageSize | integer | 否 | 20 | 每页条数 |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "items": [
      {
        "id": "tpl_001",
        "name": "AI 科普文章",
        "blocks": [
          { "type": "role", "content": "你是一位资深编辑", "order": 0 },
          { "type": "task", "content": "撰写一篇关于 AI 的科普文章", "order": 1 },
          { "type": "format", "content": "Markdown 表格", "order": 2 }
        ],
        "createdAt": "2026-08-11T10:00:00.000Z",
        "updatedAt": "2026-08-11T10:00:00.000Z"
      }
    ],
    "total": 5,
    "page": 1,
    "pageSize": 20,
    "totalPages": 1
  },
  "error": null
}
```

---

### 9.2 获取模板详情

```
GET /api/templates/:id
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "tpl_001",
    "name": "AI 科普文章",
    "blocks": [
      { "type": "role", "content": "你是一位资深编辑", "order": 0 },
      { "type": "task", "content": "撰写一篇关于 AI 的科普文章", "order": 1 }
    ],
    "createdAt": "2026-08-11T10:00:00.000Z",
    "updatedAt": "2026-08-11T10:00:00.000Z"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "模板不存在" | id 无效 |
| 403 | "无权访问该模板" | 模板不属于当前用户 |

---

### 9.3 保存模板

```
POST /api/templates
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 说明 |
|------|------|------|---------|------|
| name | string | 是 | 1—50 字符 | 模板名称 |
| blocks | Block[] | 是 | 1—20 个 | 积木块数组 |

**Block 结构：**

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| type | string | 是 | role / task / context / format / constraint |
| content | string | 是 | 1—500 字符 |
| order | integer | 是 | 0—19 |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "tpl_001",
    "name": "AI 科普文章",
    "blocks": [
      { "type": "role", "content": "你是一位资深编辑", "order": 0 }
    ],
    "createdAt": "2026-08-11T10:00:00.000Z"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "请输入模板名称" | name 为空 |
| 400 | "模板名称长度需在 1—50 字符之间" | name 超长 |
| 400 | "积木块数量需在 1—20 之间" | blocks 数量不合规 |
| 400 | "模板名称已存在，请更换" | 同名模板已存在 |

---

### 9.4 更新模板

```
PUT /api/templates/:id
```

**认证：** 是

**请求体：** 同 11.3（name、blocks）

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "tpl_001",
    "name": "AI 科普文章（改）",
    "blocks": [
      { "type": "role", "content": "你是一位资深编辑", "order": 0 }
    ],
    "updatedAt": "2026-08-11T11:00:00.000Z"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "模板不存在" | id 无效 |
| 403 | "无权修改该模板" | 模板不属于当前用户 |
| 400 | "模板名称已存在，请更换" | 改名后与其他模板冲突 |

---

### 9.5 删除模板

```
DELETE /api/templates/:id
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "tpl_001",
    "deleted": true
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 404 | "模板不存在" | id 无效 |
| 403 | "无权删除该模板" | 模板不属于当前用户 |

---

## 十、徽章模块

### 10.1 获取徽章定义列表

返回全部徽章定义及当前用户解锁状态，供个人中心徽章墙展示获取条件。

```
GET /api/badges
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "badges": [
      {
        "id": "badge_001",
        "name": "连续打卡7天",
        "description": "连续 7 天完成至少一次对话练习",
        "unlockCriteria": { "type": "streak", "target": 7 },
        "unlocked": true,
        "unlockedAt": "2026-08-07T10:00:00.000Z"
      },
      {
        "id": "badge_004",
        "name": "对话小达人",
        "description": "累计完成 10 次苏格拉底对话练习",
        "unlockCriteria": { "type": "conversation_count", "target": 10 },
        "unlocked": false,
        "unlockedAt": null
      }
    ]
  },
  "error": null
}
```

---

## 十一、个人资料模块

### 11.1 获取个人资料

```
GET /api/profile
```

**认证：** 是

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "user_test001",
    "username": "测试用户",
    "email": "test@example.com",
    "avatar": null,
    "streakDays": 7,
    "createdAt": "2026-07-01T08:00:00.000Z"
  },
  "error": null
}
```

---

### 11.2 更新个人资料

```
PUT /api/profile
```

**认证：** 是

**请求体：**

| 字段 | 类型 | 必填 | 长度/范围 | 说明 |
|------|------|------|---------|------|
| username | string | 否 | 2—30 字符 | 用户名，唯一 |
| avatar | string | 否 | — | 头像 URL |

**成功响应（200）：**

```json
{
  "success": true,
  "data": {
    "id": "user_test001",
    "username": "新用户名",
    "avatar": "https://example.com/avatar.png"
  },
  "error": null
}
```

**失败响应：**

| 状态码 | error 值 | 场景 |
|--------|---------|------|
| 400 | "用户名长度需在 2—30 字符之间" | username 不合规 |
| 400 | "该用户名已被使用" | username 与其他用户冲突 |

---

## 十二、数据模型（MySQL DDL）

以下是核心表结构（MySQL 8），由后端 `db/init.sql` 初始化，供接口开发与联调参考。

```sql
-- 用户表
CREATE TABLE users (
  id            VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'user_前缀ID',
  email         VARCHAR(100) NOT NULL COMMENT '登录邮箱',
  username      VARCHAR(30)  NOT NULL COMMENT '用户名',
  password_hash VARCHAR(255) NOT NULL COMMENT 'BCrypt 加密密码',
  avatar        VARCHAR(255) NULL COMMENT '头像URL',
  streak_days   INT          NOT NULL DEFAULT 0 COMMENT '连续打卡天数',
  created_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_email (email),
  UNIQUE KEY uk_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 苏格拉底对话表
CREATE TABLE conversations (
  id                VARCHAR(36)   NOT NULL PRIMARY KEY COMMENT 'conv_前缀ID',
  user_id           VARCHAR(36)   NOT NULL COMMENT '用户ID',
  topic_id          VARCHAR(36)   NULL COMMENT '关联知识点ID（从学习地图跳转时携带）',
  original_prompt   VARCHAR(500)  NOT NULL COMMENT '原始提示词',
  improved_prompt   TEXT          NULL COMMENT '优化后提示词',
  comparison_result JSON          NULL COMMENT '改进分析',
  rating            INT           NULL COMMENT '评分 1-5',
  status            VARCHAR(20)   NOT NULL DEFAULT 'active' COMMENT 'active / completed',
  current_round     INT           NOT NULL DEFAULT 0 COMMENT '当前轮次',
  max_rounds        INT           NOT NULL DEFAULT 5 COMMENT '最大轮次',
  created_at        DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at        DATETIME      NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_user_created (user_id, created_at),
  CONSTRAINT fk_conv_user FOREIGN KEY (user_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='苏格拉底对话表';

-- 对话消息表
CREATE TABLE messages (
  id              VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'msg_前缀ID',
  conversation_id VARCHAR(36)  NOT NULL COMMENT '对话ID',
  role            VARCHAR(20)  NOT NULL COMMENT 'user / assistant',
  content         VARCHAR(1000) NOT NULL COMMENT '消息内容',
  message_type    VARCHAR(20)  NOT NULL COMMENT 'question / answer / summary',
  created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  KEY idx_conversation (conversation_id),
  CONSTRAINT fk_msg_conv FOREIGN KEY (conversation_id) REFERENCES conversations (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='对话消息表';

-- 作品表
CREATE TABLE works (
  id         VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'work_前缀ID',
  user_id    VARCHAR(36)  NOT NULL COMMENT '用户ID',
  work_type  VARCHAR(20)  NOT NULL COMMENT 'ppt / report / email / social',
  title      VARCHAR(100) NOT NULL COMMENT '作品标题',
  content    TEXT         NULL COMMENT '生成内容',
  form_data  JSON         NOT NULL COMMENT '表单数据',
  share_url  VARCHAR(255) NULL COMMENT '分享URL',
  share_code VARCHAR(36)  NULL COMMENT '分享码',
  created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_share_code (share_code),
  KEY idx_user_created (user_id, created_at),
  CONSTRAINT fk_work_user FOREIGN KEY (user_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作品表';

-- 徽章定义表
CREATE TABLE badges (
  id              VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'badge_前缀ID',
  name            VARCHAR(50)  NOT NULL COMMENT '徽章名',
  description     VARCHAR(255) NOT NULL COMMENT '描述',
  unlock_criteria JSON         NOT NULL COMMENT '解锁条件',
  created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='徽章定义表';

-- 用户徽章关联表
CREATE TABLE user_badges (
  id          VARCHAR(36) NOT NULL PRIMARY KEY,
  user_id     VARCHAR(36) NOT NULL COMMENT '用户ID',
  badge_id    VARCHAR(36) NOT NULL COMMENT '徽章ID',
  unlocked_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '解锁时间',
  UNIQUE KEY uk_user_badge (user_id, badge_id),
  CONSTRAINT fk_ub_user  FOREIGN KEY (user_id)  REFERENCES users (id),
  CONSTRAINT fk_ub_badge FOREIGN KEY (badge_id) REFERENCES badges (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户徽章关联表';

-- 积木模板表
CREATE TABLE templates (
  id         VARCHAR(36) NOT NULL PRIMARY KEY COMMENT 'tpl_前缀ID',
  user_id    VARCHAR(36) NOT NULL COMMENT '用户ID',
  name       VARCHAR(50) NOT NULL COMMENT '模板名称',
  blocks     JSON        NOT NULL COMMENT '积木块数组',
  created_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_name (user_id, name),
  KEY idx_user_created (user_id, created_at),
  CONSTRAINT fk_tpl_user FOREIGN KEY (user_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积木模板表';
```

---

## 十三、接口总览

### 未实现接口（全部待开发）

> 当前所有接口均未实现，全部为待开发状态，共 **30** 个接口。

| 方法 | 路径 | 功能 | 认证 | 关联模块 |
|------|------|------|------|---------|
| POST | /api/auth/register | 用户注册 | 否 | 认证 |
| POST | /api/auth/login | 用户登录 | 否 | 认证 |
| POST | /api/chat | 创建苏格拉底对话 | 是 | 苏格拉底对话 |
| GET | /api/chat/history | 对话历史列表 | 是 | 苏格拉底对话 |
| GET | /api/chat/:id | 对话详情 | 是 | 苏格拉底对话 |
| POST | /api/chat/:id/message | 用户回复（AI 追问/完成） | 是 | 苏格拉底对话 |
| POST | /api/chat/:id/complete | 完成对话生成对比 | 是 | 苏格拉底对话 |
| POST | /api/chat/:id/rating | 对话评分 | 是 | 苏格拉底对话 |
| POST | /api/lab/test | 测试提示词 | 是 | 实验室 |
| GET | /api/health | 健康检查 | 否 | 基础服务 |
| GET | /api/learning/progress | 获取学习进度 | 是 | 学习地图 |
| POST | /api/learning/progress | 更新学习进度 | 是 | 学习地图 |
| GET | /api/works | 作品列表 | 是 | 作品工厂 |
| POST | /api/works | 生成作品 | 是 | 作品工厂 |
| GET | /api/works/:id | 作品详情 | 是 | 作品工厂 |
| DELETE | /api/works/:id | 删除作品 | 是 | 作品工厂 |
| POST | /api/lab/compare | 多模型对比 | 是 | 实验室 |
| POST | /api/compare | 创建智能对比 | 是 | 智能对比 |
| GET | /api/compare/history | 对比历史 | 是 | 智能对比 |
| POST | /api/templates | 保存积木模板 | 是 | 积木构建器 |
| GET | /api/templates | 模板列表 | 是 | 积木构建器 |
| GET | /api/templates/:id | 模板详情 | 是 | 积木构建器 |
| PUT | /api/templates/:id | 更新模板 | 是 | 积木构建器 |
| DELETE | /api/templates/:id | 删除模板 | 是 | 积木构建器 |
| POST | /api/works/:id/share | 生成分享链接 | 是 | 作品分享 |
| DELETE | /api/works/:id/share | 取消分享 | 是 | 作品分享 |
| GET | /api/share/:shareCode | 公开访问分享作品 | 否 | 作品分享 |
| GET | /api/badges | 徽章定义列表 | 是 | 徽章 |
| GET | /api/profile | 获取个人资料 | 是 | 个人中心 |
| PUT | /api/profile | 更新个人资料 | 是 | 个人中心 |
