# 零基础学AI平台 - 后端开发需求文档

| 项目名称 | 零基础学AI平台 |
|---------|--------------|
| 文档版本 | v3.0 |
| 更新日期 | 2026-08-12 |
| 文档状态 | 开发中 |
| 架构风格 | **功能分块（Feature-Based Modular）**，非传统全局分层 |
| 技术栈 | Java 21 (LTS) + Spring Boot 3.x + MyBatis-Plus 3.5 + MySQL 8.0 + Redis 7 / Spring Security + JWT / DeepSeek API |
| 构建部署 | Maven + Docker（前后端分离，Nginx 反向代理） |
| 关联文档 | 《开发需求文档.md》《接口文档.md》 |

---

## 一、架构总览：功能分块（Feature-Based）结构

### 1.1 设计原则

本后端**不做传统全局分层**（不设全局的 controller/、service/、mapper/ 包），而是按**业务功能**划分为一个个自包含的「功能块」。每个功能块拥有自己的 Controller、Service、Mapper、Entity、DTO、VO 与提示词模板，独立演进、独立测试。

核心原则：

| 原则 | 说明 |
|------|------|
| **功能即边界** | 一个业务域（认证、对话、作品…）一个包，包内结构自主组织 |
| **纵向切片** | 每个功能块内部仍保留「Controller → Service → Mapper」的薄分层，但作用域限定在本功能块 |
| **共享横切能力下沉** | 统一响应、全局异常、Spring Security、限流、AI 网关、工具类集中在 `common/` 与 `config/` |
| **模块间显式依赖** | 跨功能协作时显式注入对方 Service；禁止 Controller 层直连他模块表 |
| **禁止循环依赖** | 模块依赖单向：基础模块 ← 内容模块 ← 组合模块（见 1.5） |
| **稳定优先** | 生产优先：事务、熔断、监控、审计全覆盖；类型与契约编译期可查 |

### 1.2 与传统分层架构对比

| 维度 | 传统分层架构 | 功能分块架构 |
|------|------------|-------------|
| 代码组织 | 按技术角色（controllers/、services/、mappers/） | 按业务功能（modules/auth、modules/chat…） |
| 改动一个功能 | 横跨 3—5 个包 | 只动一个功能包 |
| 新人理解成本 | 先懂分层，再找业务 | 按业务名直接定位 |
| 跨模块复用 | 依赖 Service 层约定 | 显式注入目标模块 Service |
| 循环依赖风险 | 低 | 中（需约束依赖方向，见 1.5） |
| 模块卸载/扩展 | 难 | 易（新增一个包即新增一个功能） |
| 与 Spring 契合度 | 天然 | **极高**：一个功能块 ≈ 一组 @Component + 一组 Mapper，Spring 容器自动装配 |

### 1.3 工程与包结构

```
ai-platform/
├── pom.xml                        # Maven 依赖
├── db/
│   └── init.sql                   # 建表脚本（见第五章）
├── docker-compose.yml             # 前端 Nginx + 后端 + MySQL + Redis
├── Dockerfile                     # 后端镜像（JAR）
├── nginx/
│   └── nginx.conf                 # 前后端分离反向代理
└── src/main/java/com/aiplatform/
    ├── AiPlatformApplication.java # Spring Boot 启动类
    ├── common/                    # 【共享】统一响应/异常/工具
    │   ├── Result.java            # 统一响应 {success,data,error}
    │   ├── ResultCode.java        # 错误码枚举
    │   ├── BizException.java      # 业务异常（携带状态码+中文消息）
    │   ├── GlobalExceptionHandler.java  # @RestControllerAdvice 全局异常
    │   ├── PageQuery.java         # 分页入参封装
    │   └── util/                  # ID 生成、JWT 工具等
    ├── config/                    # 【共享】框架配置
    │   ├── SecurityConfig.java    # Spring Security + JWT 过滤链
    │   ├── RedisConfig.java       # RedisTemplate 序列化
    │   ├── MybatisPlusConfig.java # 分页插件、乐观锁、字段自动填充
    │   ├── RateLimitConfig.java   # 限流注解 AOP
    │   └── AiConfig.java          # DeepSeek 配置（模型/baseURL/超时）
    ├── ai/                        # 【共享】AI 网关（唯一 AI 出口）
    │   ├── AiGateway.java         # 对外唯一入口（含配额/熔断/重试/JSON解析）
    │   ├── AiHttpClient.java      # DeepSeek HTTP 调用（RestClient）
    │   └── AiJsonParser.java      # 响应 JSON 清理与解析
    └── modules/                   # 【功能块】每个功能一个包
        ├── auth/                  # 认证
        ├── health/                # 健康检查
        ├── chat/                  # 苏格拉底对话（核心）
        ├── learning/              # 学习地图 / 进度
        ├── works/                 # 作品工厂
        ├── share/                 # 作品分享
        ├── templates/             # 积木构建器模板
        ├── lab/                   # 提示词实验室
        ├── compare/               # 智能对比
        ├── badges/                # 徽章
        └── profile/               # 个人资料
```

**每个功能块内部的标准结构**（以 `chat/` 为例）：

```
modules/chat/
├── ChatController.java     # REST 层：取参 + @Validated 校验 + 调 Service + 包装 Result
├── ChatService.java        # 业务逻辑：状态机、AI 编排、事务、联动
├── mapper/
│   ├── ConversationMapper.java   # extends BaseMapper<Conversation>
│   └── MessageMapper.java
├── entity/                 # 数据库实体（与表字段一一对应）
│   ├── Conversation.java
│   └── Message.java
├── dto/                    # 请求/内部传输对象
├── vo/                     # 响应视图对象
└── prompt/                 # 本块专属 AI 提示词模板
    └── SocraticPrompts.java
```

### 1.4 请求处理链路

```
HTTP 请求
  → Nginx 反向代理 /api/* → 后端 :8080
  → Filter：CORS、请求日志
  → Spring Security FilterChain：JWT 认证（白名单放行）
  → 限流 AOP：Redis 计数（IP 级 60/min，用户级 50 次/天，实验 3 次/min）
  → Controller：@Validated 参数校验
  → Service：业务逻辑 + AI 网关调用 + @Transactional 落库
  → Mapper：MyBatis-Plus 预编译 SQL
  → 统一返回 Result<T>
异常 → GlobalExceptionHandler：统一映射为 {success:false, error:"中文提示"}
```

### 1.5 模块依赖方向（禁止循环）

```
基础层（无依赖）         auth, health, templates, lab + common/config/ai
     │
内容层（依赖基础）       works, share, compare, learning, badges
     │
组合层（依赖内容）       chat（→ learning）
```

**依赖纪律：**
- 低层模块**不得**注入高层模块 Service（如 `learning` 不得依赖 `chat`）。
- 跨块调用只允许注入对方 **Service 公开方法**，禁止直接使用对方 Mapper。
- 跨块只读聚合由调用方自行查询，或通过对方 Service 只读方法获取。

---

## 二、全局横切需求（common/ + config/ + ai/）

### 2.1 配置管理

| 配置项 | 配置键 | 说明 |
|--------|--------|------|
| 服务端口 | `server.port` | 8080 |
| 数据库 | `spring.datasource.*` | MySQL 8，连接池 HikariCP（初始/最大 5/20） |
| Redis | `spring.data.redis.*` | 限流、配额、分布式锁 |
| JWT 密钥/有效期 | `jwt.secret` / `jwt.expire` | 生产 ≥32 位随机，有效期 7 天 |
| DeepSeek | `ai.deepseek.*` | baseURL、apiKey、model、timeout(30s) |
| 日 AI 配额 | `ai.daily-limit` | 单用户 50 次/天 |
| IP 限流 | `ratelimit.ip` | 60 次/分钟 |

**安全约定**：`apiKey`、`jwt.secret`、数据库密码一律走环境变量/Spring profile，不入库、不入代码库；生产禁用 Swagger。

### 2.2 统一响应与全局异常

```java
// common/Result.java
public class Result<T> {
    private boolean success;
    private T data;
    private String error;
    public static <T> Result<T> ok(T data) { ... }      // {success:true, data, error:null}
    public static <T> Result<T> fail(String msg) { ... } // {success:false, data:null, error:msg}
}

// common/BizException.java
public class BizException extends RuntimeException {
    private final int status;   // 400/403/404/429/500
    private final String message;
}
```

**错误分类约定：**

| 类型 | 抛出 | 最终状态码 |
|------|------|-----------|
| 参数校验失败 | 框架 @Valid 异常 | 400 |
| 业务规则 | `throw new BizException(400/403/404, "中文提示")` | 400/403/404 |
| 未认证 / token 失效 | Security 拒绝 | 401 |
| 限流 | RateLimit 异常 | 429 |
| AI 服务异常 | `BizException(500, "AI 服务暂时不可用，请稍后重试")` | 500 |
| 未知异常 | 兜底，日志 + 500 | 500 |

`GlobalExceptionHandler` 用 `@RestControllerAdvice` 统一捕获：`MethodArgumentNotValidException → 400`、`BizException → 对应码`、`Exception → 500（记录堆栈，响应不含技术细节）`。

### 2.3 认证：Spring Security + JWT

```java
// config/SecurityConfig.java
@Configuration
@EnableWebSecurity
public class SecurityConfig {
    // permitAll：POST /api/auth/register、POST /api/auth/login、
    //          GET /api/health、GET /api/share/{shareCode}、Swagger(仅dev)
    // 其余所有请求需认证（无状态 JWT，不建 Session）
}
```

**JWT 过滤器流程**：解析 `Authorization: Bearer <token>` → 校验签名/过期 → 写入 `SecurityContext`（principal = userId）→ Controller 通过 `@AuthenticationPrincipal` 或 `SecurityContextHolder` 取当前用户。token 失效/过期 → 401。

### 2.4 Redis 限流与 AI 配额

| 维度 | Key 设计 | 规则 |
|------|---------|------|
| IP 全局 | `rl:ip:{ip}:{minute}` | 60 次/分钟，`INCR + EXPIRE` |
| 登录失败 | `rl:login:{email}:{ip}` | 连续失败 5 次锁 15 分钟 |
| 用户 AI 配额 | `ai:usage:{userId}:{yyyyMMdd}` | 50 次/天 |
| 实验室实验 | `rl:lab:{userId}:{minute}` | 3 次/分钟 |

用自定义注解 `@RateLimit(type=..., limit=..., window=...)` + AOP 实现，作用于 Controller 方法。

### 2.5 AI 网关（唯一 AI 出口，ai/）

```java
@Service
public class AiGateway {
    public <T> T chatJson(String userId, List<ChatMessage> messages, Class<T> clazz)
        throws BizException {
        // 1. 配额检查：Redis INCR ai:usage:{userId}:{date}，超过 ai.daily-limit → 429
        // 2. 调用 AiHttpClient（RestClient → POST /chat/completions，
        //     model=deepseek-chat, temperature, max_tokens, response_format={type:"json_object"}）
        // 3. 超时 30s；Resilience4j 熔断（失败率>50% 熔断 30s）+ 重试 1 次
        // 4. AiJsonParser 剥离 ```json 围栏后解析为 T；解析失败重试 1 次，仍失败 → 500
        // 5. AI 失败 → BizException(500, "AI 服务暂时不可用，请稍后重试")
    }

    public String chatText(String userId, List<ChatMessage> messages, AiParams params) { ... } // 文本流
}
```

- **全站 AI 调用只能走 AiGateway**：天然统一配额、熔断、降级。
- **降级策略**：DeepSeek 不可用 → 对话模块提示"AI 服务暂时不可用"，其他模块不受影响；多模型对比中单模型失败不影响其他模型。

### 2.6 参数校验（Bean Validation）

- DTO 字段加 `@NotBlank` / `@Size` / `@Email` / `@Range`，Controller 参数加 `@Validated`。
- 校验失败由 `GlobalExceptionHandler` 统一转 400，消息取约束注解的 `message`（中文）。
- 分页参数统一封装 `PageQuery`（page/pageSize，pageSize≤100）。

### 2.7 日志与监控

- **日志**：Logback + SLF4J；JSON 格式；请求日志（method/path/status/耗时/userId）+ 业务日志 + 错误日志（含堆栈，仅服务端）。
- **监控**：Spring Actuator 暴露 `/actuator/health`、`/actuator/metrics`；Micrometer 导出 Prometheus 指标（QPS、P95 延迟、JVM、AI 调用失败率、Redis 命中）。
- **链路**：日志 traceId 贯穿（Filter 生成，响应头回传），便于定位单次请求全链路。

---

## 三、功能模块实现分析

> 每个功能块统一模板：功能概述 → 文件结构 → 数据模型 → 接口清单 → 开发流程拆解 → 关键实现 → 边界/异常 → 测试要点。

---

### 3.1 auth 认证功能块

**功能概述**：注册、登录、签发 JWT。所有业务功能块的前置。

**文件结构**：`modules/auth/`（AuthController、AuthService、UserMapper、entity/User、dto/RegisterReq|LoginReq、vo/LoginVO）

**数据模型**：`users` 表（第五章）。

**接口清单**：
| 接口 | 认证 | 说明 |
|------|------|------|
| POST /api/auth/register | 否 | 注册成功自动登录（返回 token） |
| POST /api/auth/login | 否 | 登录，返回 token + 用户信息 |

**开发流程拆解**：
1. **T1 注册**：@Valid 校验 → 查重（email、username）→ `BCryptPasswordEncoder` 加密 → 插入 User → 签发 JWT → 返回 LoginVO。
2. **T2 登录**：查 User → 比对密码 → 失败计数（Redis `rl:login`，连续 5 次锁 15 分钟）→ 成功清除计数 → 签发 JWT。
3. **T3 防泄露**：登录失败统一"邮箱或密码错误"，不区分字段。

**关键实现**：
```java
@PostMapping("/register")
public Result<LoginVO> register(@Valid @RequestBody RegisterReq req) {
    if (userMapper.existsByEmail(req.getEmail())) throw new BizException(400, "该邮箱已注册");
    if (userMapper.existsByUsername(req.getUsername())) throw new BizException(400, "该用户名已被使用");
    User user = userService.createUser(req, encoder.encode(req.getPassword()));
    return Result.ok(LoginVO.of(user, jwtUtil.generate(user.getId())));
}
```

**边界/异常**：邮箱格式/长度/唯一；密码强度；账号锁定 15 分钟；token 过期 401。

**测试要点**：注册成功/重复邮箱/重复用户名；登录成功/密码错/连续失败锁定；JWT 过期。

---

### 3.2 health 健康检查功能块

**功能概述**：探活接口，暴露服务与依赖状态。

**文件结构**：`modules/health/HealthController.java`（单文件）

**接口**：`GET /api/health`（免鉴权）→ `{ status, services:{database, redis, deepseek}, version }`

**开发流程拆解**：
1. **T1**：Actuator 数据库健康检查（`dataSource` health indicator）。
2. **T2**：Redis ping（`redisTemplate.getConnectionFactory().getConnection().ping()`）。
3. **T3**：DeepSeek 短超时探测，仅返回 `available/unavailable`。

**测试要点**：DB/Redis 断开时对应服务标记不可用但接口仍 200。

---

### 3.3 chat 苏格拉底对话功能块（核心）

**功能概述**：用户输入模糊需求，AI 苏格拉底式追问补全五要素，信息充足后生成优化提示词与改进分析；评分联动学习进度。

**文件结构**：
```
modules/chat/
├── ChatController.java
├── ChatService.java          # 追问状态机 + AI 编排 + 事务
├── mapper/ConversationMapper.java / MessageMapper.java
├── entity/Conversation.java / Message.java
├── dto/CreateChatReq.java / ReplyReq.java / RatingReq.java
├── vo/ChatTurnVO.java / ChatDetailVO.java / CompleteVO.java
└── prompt/SocraticPrompts.java
```

**数据模型**：`conversations`、`messages` 表。

**接口清单**：POST /api/chat、GET /api/chat/history、GET /api/chat/:id、POST /api/chat/:id/message、POST /api/chat/:id/complete、POST /api/chat/:id/rating。

**开发流程拆解**：
1. **T1**：创建对话——校验 originalPrompt 1-500、查无 active 对话 → 插入 conversation + 首条 user 消息 → AI 生成首轮追问（question）。
2. **T2 状态机**：回复消息——存 user 消息 → 组装上下文 → AiGateway.chatJson 返回 `{action, question, confidence, ...}`。
3. **T3 停止判断**：`confidence≥80` 或 `currentRound≥5` 或用户点完成 → 完成流程。
4. **T4 完成**：AI 生成 improvedPrompt + comparisonResult → `@Transactional` 更新 conversation + 存 summary 消息。
5. **T5 评分联动**：更新 rating → 调 `learningService.updateProgress(userId, topicId, rating)` → 返回平均分/掌握数。

**关键实现（追问状态机）**：

`SocraticPrompts.system()`：
> 你是苏格拉底式提示词工程老师。逐轮追问用户提示词缺失要素，每轮只聚焦一个维度，先指出缺失再提问。信息完整度 confidence≥80 时停止追问，生成优化提示词与改进分析。必须返回 JSON：{action, question, confidence}。

```java
@Transactional
public TurnVO reply(String userId, String conversationId, String content) {
    Conversation conv = requireOwnedActive(userId, conversationId);  // 404/403/400
    messageMapper.insert(Message.user(userId, conversationId, content));

    AiTurn turn = aiGateway.chatJson(userId,
        buildMessages(conv, content), AiTurn.class);

    if ("ask".equals(turn.getAction())) {
        messageMapper.insert(Message.assistant(conversationId, turn.getQuestion(), "question"));
        conversationMapper.incrementRound(conversationId);
        return TurnVO.continueTurn(turn.getQuestion());
    }
    return complete(conv, turn);   // action=complete → 生成优化结果并落库
}
```

**并发控制**：创建对话前查 `status='active'` 的存在性，存在则 400"你有一个进行中的对话，请先完成"。

**边界/异常**：空输入拦截；AI 超时>30s 可重试；5 轮强制完成；completed 状态不可再回复/评分；403 防越权。

**测试要点**：追问轮数上限；AI 非法 JSON 重试；并发 active 限制；评分后进度同步；越权 403。

---

### 3.4 learning 学习地图/进度功能块

**功能概述**：四阶段技能树、知识点状态机与阶段解锁；由 chat 评分驱动；提供进度查询。

**文件结构**：
```
modules/learning/
├── LearningController.java      # 仅 GET /api/learning/progress 对外
├── LearningService.java         # updateProgress / 阶段解锁（可被 chat 注入）
├── mapper/KnowledgePointMapper.java / UserKnowledgeProgressMapper.java
├── entity/KnowledgePoint.java / UserKnowledgeProgress.java
├── config/LearningConfig.java   # 四阶段知识点与阈值配置（种子来源）
└── vo/LearningProgressVO.java
```

**数据模型**：`knowledge_points`、`user_knowledge_progress` 表（见第五章）。

**接口清单**：
| 接口 | 说明 |
|------|------|
| GET /api/learning/progress | 统计卡片 + 技能树 + 知识点状态 |
| （内部方法）updateProgress | 由 chat 评分在 Service 层调用，不暴露 REST |

**开发流程拆解**：
1. **T1 种子初始化**：LearningConfig 定义四阶段（阈值 3.5/4.0/4.5）与知识点，启动时或 seed 脚本写入 knowledge_points。
2. **T2 进度查询**：聚合 conversations.rating（最近 20 条平均）、users.streak_days、user_knowledge_progress、user_badges → 拼装 stages。
3. **T3 状态机**：`updateProgress(userId, topicId, rating)`：rating≥3.5 → mastered（记录 best_rating）。
4. **T4 阶段解锁**：阶段内全部 mastered 且平均分达阈值 → 下一阶段解锁。

**关键实现**：
```java
@Transactional
public UpdateProgressVO updateProgress(String userId, String topicId, int rating) {
    KnowledgePoint kp = knowledgePointMapper.selectById(topicId);
    UserKnowledgeProgress prev = progressMapper.selectBy(userId, kp.getId()); // 或 new
    String status = rating >= kp.getUnlockThreshold()
        ? "mastered" : "mastered".equals(prev.getStatus()) ? "mastered" : "learning";
    int best = Math.max(prev.getBestRating(), rating);
    progressMapper.upsert(userId, kp.getId(), status, best);

    if ("mastered".equals(status) && !"mastered".equals(prev.getStatus())) {
        maybeUnlockNextStage(userId, kp.getStage());   // 整阶段检查 → 解锁下一阶段
    }
    return new UpdateProgressVO(status, best, stageUnlocked);
}
```

**边界/异常**：新用户全锁定、平均分"—"；解锁判定以后端为准。

**测试要点**：评分 3.5 边界；整阶段掌握解锁；平均分不足不误解锁。

---

### 3.5 works 作品工厂功能块

**功能概述**：按模板（PPT/报告/邮件/社交）填表单 → AI 生成内容 → 落库作品库。

**文件结构**：`modules/works/`（WorksController、WorksService、WorkMapper、entity/Work、dto/CreateWorkReq、prompt/WorkPrompts、vo/WorkVO）

**数据模型**：`works` 表（含 share_code）。

**接口清单**：GET/POST /api/works、GET/DELETE /api/works/:id。

**开发流程拆解**：
1. **T1 模板编排**：WorkPrompts 按 workType 拼装系统提示（邮件按收件人调整称呼措辞等）。
2. **T2 动态校验**：按 workType 校验 formData 必填字段（ppt 需 topic/audience…，email 需 recipient/purpose/tone…）。
3. **T3 生成**：AiGateway.chatText → content 落库（空/异常不计入作品库）。
4. **T4 列表/详情/删除**：按 userId 过滤，403 防越权，分页。

**边界/异常**：formData 缺字段 400；AI 超时>30s 重试；超长内容截断提示；空历史空状态。

**测试要点**：四类模板 formData 校验；AI 空返回不落库；越权 403。

---

### 3.6 share 作品分享功能块

**功能概述**：为作品生成公开分享码，免登录查看；取消分享；公开访问不泄露隐私。

**文件结构**：`modules/share/`（ShareController、ShareService、WorkMapper（复用）、vo/ShareVO、vo/PublicWorkVO）

**数据模型**：复用 `works.share_code/share_url`。

**接口清单**：
| 接口 | 认证 | 说明 |
|------|------|------|
| POST /api/works/:id/share | 是 | 生成分享码（无则生成，有则复用） |
| DELETE /api/works/:id/share | 是 | 取消分享 |
| GET /api/share/:shareCode | 否 | 公开读取作品内容 + 作者名 |

**开发流程拆解**：
1. **T1 生成**：校验作品归属 → 生成 6 位短码（随机，冲突重试）→ 更新 share_code/share_url。
2. **T2 公开读**：按 shareCode 查作品 + 作者 username → 只返回 title/workType/content/author/createdAt，**剥离 userId/email 等敏感字段**。
3. **T3 取消**：清空 share_code/share_url。

**边界/异常**：shareCode 无效或已取消 → 404；公开接口已在 Security 白名单。

**测试要点**：公开访问不含敏感字段；取消后链接 404；越权生成 403。

---

### 3.7 templates 积木构建器模板功能块

**功能概述**：积木块拼装结果保存为个人模板，CRUD + 同名拦截。

**文件结构**：`modules/templates/`（TemplateController、TemplateService、TemplateMapper、entity/Template、dto/SaveTemplateReq、vo/TemplateVO）

**数据模型**：`templates` 表（user_id+name 唯一、blocks JSON）。

**接口清单**：GET/POST /api/templates、GET/PUT/DELETE /api/templates/:id。

**开发流程拆解**：
1. **T1 保存**：校验 name 1-50、blocks 1-20、块内 type 枚举/content 1-500/order 0-19 → 查同名 → 插入。
2. **T2 列表/详情**：按 userId 过滤 + 分页。
3. **T3 更新/删除**：归属校验 + 改名冲突校验。

**边界/异常**：同名 400；积木超 20 个 400；越权 403。

**测试要点**：同名唯一约束；blocks 结构校验；更新改名冲突。

---

### 3.8 lab 提示词实验室功能块

**功能概述**：单提示词测试（DeepSeek）+ 多模型对比（预留 GPT-4o/Claude）。温度/maxTokens 参数 + 用户级限流。

**文件结构**：`modules/lab/`（LabController、LabService、vo/LabResultVO）

**接口清单**：
| 接口 | 说明 |
|------|------|
| POST /api/lab/test | 单模型测试，返回 result/usage/duration |
| POST /api/lab/compare | 多模型并排，部分失败降级（各模型独立 status） |

**开发流程拆解**：
1. **T1 test**：校验 prompt 1-4000、temperature 0-2、maxTokens 1-8192 → AiGateway.chatText → 返回结果 + 耗时。
2. **T2 compare**：并发调用各模型（`CompletableFuture` + `allOf`），每模型独立 success/failed；全部失败才报错。
3. **T3 限流**：`@RateLimit(type=LAB, limit=3, window=60s)` → 超出 429。
4. **T4 降级**：仅 DeepSeek 可用时，compare 退化为不同温度的多参数对比。

**边界/异常**：未选模型 400；单模型失败不影响其他；全部失败重试。

**测试要点**：compare 部分失败仍 200；限流 429；参数越界 400。

---

### 3.9 compare 智能对比功能块

**功能概述**：两段提示词差异分析 + 改进建议 + 得分对比。

**文件结构**：`modules/compare/`（CompareController、CompareService、ComparisonMapper、entity/Comparison、dto/CompareReq、vo/CompareVO）

**数据模型**：`comparisons` 表。

**接口清单**：POST /api/compare、GET /api/compare/history。

**开发流程拆解**：
1. **T1 分析**：校验两段 1-2000 → AiGateway.chatJson → `{improvements:[{dimension,original,improved,impact}], scoreComparison:{...}}`。
2. **T2 落库**：写入 comparisons（id 前缀 cmp_）。
3. **T3 历史**：分页返回摘要字段。

**边界/异常**：任一段为空/超长 400；AI 失败可重试。

**测试要点**：输入边界；历史分页。

---

### 3.10 badges 徽章功能块

**功能概述**：徽章定义 + 解锁引擎。被 chat/works 等通过 checkAndUnlock 触发；提供定义列表。

**文件结构**：`modules/badges/`（BadgeController、BadgeService、BadgeMapper、UserBadgeMapper、entity/Badge|UserBadge、vo/BadgeVO）

**数据模型**：`badges`、`user_badges` 表。

**接口清单**：
| 接口 | 说明 |
|------|------|
| GET /api/badges | 全部徽章定义 + 当前用户解锁状态 |

**开发流程拆解**：
1. **T1 解锁引擎**：`checkAndUnlock(userId, type, value)`：查 criteria.type=type 且未解锁，value≥target → 插入 user_badges。
2. **T2 触发埋点**：chat（对话数、首次评分、连续打卡）、works（作品数）。
3. **T3 列表**：返回定义 + unlocked/unlockedAt。

**关键实现**：
```java
@Transactional
public void checkAndUnlock(String userId, String type, int value) {
    for (Badge badge : badgeMapper.selectByCriteriaType(type)) {
        if (!userBadgeMapper.exists(userId, badge.getId())
                && value >= badge.getUnlockTarget()) {
            userBadgeMapper.insert(new UserBadge(userId, badge.getId()));
        }
    }
}
```

**徽章类型**（seed）：`streak`（连续打卡 7 天）、`conversation_count`、`first_rating`、`stage_complete`、`works_count`。

**测试要点**：达标解锁；重复触发不重复创建；不同类型互不影响。

---

### 3.11 profile 个人资料功能块

**功能概述**：维护用户账号资料（用户名、头像），提供当前用户信息查询。

**文件结构**：`modules/profile/`（ProfileController、ProfileService、UserMapper（复用）、dto/UpdateProfileReq、vo/UserProfileVO）

**接口清单**：GET /api/profile、PUT /api/profile。

**开发流程拆解**：
1. **T1 获取**：按 userId 查 User → 返回 id/username/email/avatar/streakDays/createdAt（不含 password_hash）。
2. **T2 更新**：可选 username/avatar；username 2-30 且全局唯一（`update` 前查重排除自己）。
3. **T3 敏感防护**：任何接口不返回 password_hash。

**边界/异常**：username 冲突 400；长度不合规 400。

**测试要点**：username 唯一冲突；不泄露 password_hash。

---

## 四、功能块间数据流（端到端场景）

**场景：完成一次苏格拉底对话并评分**
```
POST /api/chat → ChatService（建对话 + 首轮追问）
  → 多轮 POST /:id/message（ChatService 状态机 + AiGateway）
  → POST /:id/complete（生成优化结果，status=completed）
  → POST /:id/rating
      → ChatService 更新 rating
      → LearningService.updateProgress(userId, topicId, rating)   // 掌握状态 + 阶段解锁
      → 返回平均分/掌握数
```

**场景：生成并分享作品**
```
POST /api/works → WorksService（模板拼装 → AiGateway 生成 → 落库）
  → POST /api/works/:id/share → ShareService（生成 shareCode）
  → 公开访问 GET /api/share/{code}（免鉴权，返回脱敏作品）
```

---

## 五、数据库设计（MySQL 8 DDL）

> 完整建表脚本见 `db/init.sql`；与《接口文档》十二章节一致，另含登录失败计数、AI 配额辅助表。

```sql
-- 用户表
CREATE TABLE users (
  id            VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'user_前缀ID',
  email         VARCHAR(100) NOT NULL COMMENT '登录邮箱',
  username      VARCHAR(30)  NOT NULL COMMENT '用户名',
  password_hash VARCHAR(255) NOT NULL COMMENT 'BCrypt 加密密码',
  avatar        VARCHAR(255) NULL COMMENT '头像URL',
  streak_days   INT          NOT NULL DEFAULT 0 COMMENT '连续打卡天数',
  created_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_email (email),
  UNIQUE KEY uk_username (username)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';

-- 登录失败计数（锁定）
CREATE TABLE login_attempts (
  id        BIGINT       NOT NULL AUTO_INCREMENT PRIMARY KEY,
  email     VARCHAR(100) NOT NULL COMMENT '登录邮箱',
  ip        VARCHAR(45)  NOT NULL COMMENT '来源IP',
  failed_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  KEY idx_email_ip (email, ip)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='登录失败记录';

-- 苏格拉底对话表
CREATE TABLE conversations (
  id                VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'conv_前缀ID',
  user_id           VARCHAR(36)  NOT NULL COMMENT '用户ID',
  original_prompt   VARCHAR(500) NOT NULL COMMENT '原始提示词',
  improved_prompt   TEXT         NULL COMMENT '优化后提示词',
  comparison_result JSON         NULL COMMENT '改进分析',
  rating            INT          NULL COMMENT '评分 1-5',
  status            VARCHAR(20)  NOT NULL DEFAULT 'active' COMMENT 'active / completed',
  current_round     INT          NOT NULL DEFAULT 0 COMMENT '当前轮次',
  max_rounds        INT          NOT NULL DEFAULT 5 COMMENT '最大轮次',
  created_at        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at        DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  KEY idx_user_created (user_id, created_at),
  CONSTRAINT fk_conv_user FOREIGN KEY (user_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='苏格拉底对话表';

-- 对话消息表
CREATE TABLE messages (
  id              VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'msg_前缀ID',
  conversation_id VARCHAR(36)  NOT NULL COMMENT '对话ID',
  role            VARCHAR(20)  NOT NULL COMMENT 'user / assistant',
  content         VARCHAR(1000) NOT NULL COMMENT '消息内容',
  message_type    VARCHAR(20)  NOT NULL COMMENT 'question / answer / summary',
  created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  KEY idx_conversation (conversation_id),
  CONSTRAINT fk_msg_conv FOREIGN KEY (conversation_id) REFERENCES conversations (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='对话消息表';

-- 学习知识点（配置）
CREATE TABLE knowledge_points (
  id               VARCHAR(36) NOT NULL PRIMARY KEY COMMENT 'topic_前缀ID',
  stage            VARCHAR(20) NOT NULL COMMENT 'beginner/intermediate/advanced/master',
  name             VARCHAR(50) NOT NULL COMMENT '知识点名',
  sort_order       INT         NOT NULL COMMENT '阶段内排序',
  unlock_threshold FLOAT       NOT NULL DEFAULT 3.5 COMMENT '掌握所需评分阈值',
  KEY idx_stage_order (stage, sort_order)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='学习知识点配置';

-- 用户知识点进度
CREATE TABLE user_knowledge_progress (
  id                VARCHAR(36) NOT NULL PRIMARY KEY,
  user_id           VARCHAR(36) NOT NULL COMMENT '用户ID',
  knowledge_point_id VARCHAR(36) NOT NULL COMMENT '知识点ID',
  status            VARCHAR(20) NOT NULL DEFAULT 'locked' COMMENT 'locked/learning/mastered',
  best_rating       FLOAT       NULL COMMENT '历史最高评分',
  updated_at        DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_kp (user_id, knowledge_point_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户知识点进度';

-- 作品表
CREATE TABLE works (
  id         VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'work_前缀ID',
  user_id    VARCHAR(36)  NOT NULL COMMENT '用户ID',
  work_type  VARCHAR(20)  NOT NULL COMMENT 'ppt / report / email / social',
  title      VARCHAR(100) NOT NULL COMMENT '作品标题',
  content    TEXT         NULL COMMENT '生成内容',
  form_data  JSON         NOT NULL COMMENT '表单数据',
  share_url  VARCHAR(255) NULL COMMENT '分享URL',
  share_code VARCHAR(36)  NULL COMMENT '分享码',
  created_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_share_code (share_code),
  KEY idx_user_created (user_id, created_at),
  CONSTRAINT fk_work_user FOREIGN KEY (user_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='作品表';

-- 积木模板表
CREATE TABLE templates (
  id         VARCHAR(36) NOT NULL PRIMARY KEY COMMENT 'tpl_前缀ID',
  user_id    VARCHAR(36) NOT NULL COMMENT '用户ID',
  name       VARCHAR(50) NOT NULL COMMENT '模板名称',
  blocks     JSON        NOT NULL COMMENT '积木块数组',
  created_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_name (user_id, name),
  KEY idx_user_created (user_id, created_at),
  CONSTRAINT fk_tpl_user FOREIGN KEY (user_id) REFERENCES users (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='积木模板表';

-- 智能对比记录
CREATE TABLE comparisons (
  id               VARCHAR(36) NOT NULL PRIMARY KEY COMMENT 'cmp_前缀ID',
  user_id          VARCHAR(36) NOT NULL COMMENT '用户ID',
  original_prompt  VARCHAR(2000) NOT NULL COMMENT '原始提示词',
  compared_prompt  VARCHAR(2000) NOT NULL COMMENT '待对比提示词',
  analysis         JSON        NOT NULL COMMENT '差异分析结果',
  created_at       DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP,
  KEY idx_user_created (user_id, created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='智能对比记录';

-- 徽章定义表
CREATE TABLE badges (
  id              VARCHAR(36)  NOT NULL PRIMARY KEY COMMENT 'badge_前缀ID',
  name            VARCHAR(50)  NOT NULL COMMENT '徽章名',
  description     VARCHAR(255) NOT NULL COMMENT '描述',
  unlock_criteria JSON         NOT NULL COMMENT '解锁条件',
  created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='徽章定义表';

-- 用户徽章关联表
CREATE TABLE user_badges (
  id          VARCHAR(36) NOT NULL PRIMARY KEY,
  user_id     VARCHAR(36) NOT NULL COMMENT '用户ID',
  badge_id    VARCHAR(36) NOT NULL COMMENT '徽章ID',
  unlocked_at DATETIME    NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '解锁时间',
  UNIQUE KEY uk_user_badge (user_id, badge_id),
  CONSTRAINT fk_ub_user  FOREIGN KEY (user_id)  REFERENCES users (id),
  CONSTRAINT fk_ub_badge FOREIGN KEY (badge_id) REFERENCES badges (id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户徽章关联表';
```

**索引策略**：users(email/username) 唯一、conversations(user_id+created_at)、messages(conversation_id)、works(user_id+created_at)、works(share_code) 唯一、templates(user_id+name) 唯一、user_badges(user_id+badge_id) 唯一、comparisons(user_id+created_at)、ai_usage(user_id+date) 唯一。

**种子数据**：`knowledge_points`（四阶段知识点）、`badges`（徽章定义）由启动 `ApplicationRunner` 或 seed 脚本初始化。

---

## 六、开发实施顺序

| 阶段 | 内容 | 功能块 | 前置 |
|------|------|--------|------|
| 1 | 基础设施 | Maven 工程、common/config、Security+JWT、MySQL DDL、health、auth | 无 |
| 2 | 核心学习链路 | lab(test) → chat → learning | 阶段 1（依赖 AiGateway） |
| 3 | 内容生产 | works → templates → share | 阶段 1 |
| 4 | 奖励与对比 | badges → compare | 阶段 2 |
| 5 | 收尾加固 | AI 网关熔断、Redis 限流、监控告警、性能优化、Docker 部署 | 全部 |

每个功能块验收标准：**接口契约与《接口文档》一致 → 边界用例通过 → JUnit 测试通过 → 前端联调无 Mock**。

---

## 七、测试与验收策略

| 层级 | 工具 | 覆盖 |
|------|------|------|
| 单元 | JUnit 5 + Mockito | Service 纯逻辑（状态机、解锁、配额） |
| 集成 | Spring Boot Test + MockMvc | 每功能块路由：正常流 + 边界 + 403/404/401/429 |
| 数据 | Testcontainers（MySQL 8） | schema 约束（唯一索引、外键） |
| AI 依赖 | Mock AiGateway | 固定 JSON 验证解析/重试/降级；超时模拟 |
| 安全 | Spring Security 测试 | 白名单/鉴权/越权 |
| 验收 | 接口文档逐条核对 | 30 个接口请求/响应结构一致 |

**AI 关键测试**：AiGateway JSON 解析失败重试 1 次；30s 超时提示；熔断降级；苏格拉底停止判断（confidence≥80 / 5 轮上限）。

---

## 八、部署方案（前后端分离）

### 8.1 架构形态

```
客户端（浏览器）
  ↓ HTTPS
Nginx（:443）
  ├── /            → 前端静态资源（React build 产物）
  └── /api/*       → 反向代理 http://backend:8080
  └── /actuator/*  → 反向代理到监控（内网/IP 白名单）
backend（Spring Boot JAR, 容器内 :8080）
  ├── MySQL 8（独立容器/云 RDS）
  └── Redis 7（独立容器/云 Redis）
```

### 8.2 docker-compose 服务清单

| 服务 | 镜像 | 说明 |
|------|------|------|
| frontend | nginx:alpine | 挂载 React 构建产物 + nginx.conf |
| backend | openjdk:21 + 构建 JAR | 环境变量注入 JWT_SECRET / DEEPSEEK_API_KEY / DB / Redis |
| mysql | mysql:8.0 | 挂载 db/init.sql，数据卷持久化，utf8mb4 |
| redis | redis:7-alpine | AOF 持久化 |

### 8.3 上线加固清单

- 强制 HTTPS（Let's Encrypt，Nginx 配置证书）。
- JWT_SECRET、DEEPSEEK_API_KEY、数据库密码全部走环境变量（Docker secret / 云平台密钥管理）。
- 生产关闭 Swagger、关闭 Actuator 敏感端点（或加 IP 白名单）。
- 前端 CORS 仅放行前端域名；`/api` 全部经 Nginx 反代，不直接暴露后端端口。
- 监控：Prometheus 抓取 backend 指标 + Grafana 看板（QPS、错误率、AI 调用失败率、Redis 内存）。
- 备份：MySQL 每日定时全量备份 + binlog；Redis AOF。
- 限流：单 IP 60/min、单用户 AI 50 次/天、登录失败锁定，防止刷接口与滥用。
