# 版本迭代记录

> 平台：零基础学 AI（Spark · 提问的深度，决定答案的高度）
> 作者：范烨
> 说明：滚动迭代日志，按批次追加。批次索引：2026-09-13（缺陷修复：苏格拉底对话 / 登录页假账号 / 密码框眼睛）· 2026-09-13（Phase 3 用户管理 / API 配置 / 监控 — 三项需求收口）· 2026-09-13（Phase 2 头像审核 + 后台骨架）· 2026-09-13（Phase 1 全局 AI 助手）· 2026-09-13（Phase 0 地基）· 2026-09-07（页面分址）· 2026-09-06（苏格拉底实时评分 + 模块调整）

---

## 迭代批次 · 2026-09-13（缺陷修复：苏格拉底对话 / 登录页假账号 / 密码框）

> 类型：`fix(frontend/backend)` 用户验收时反馈的三个问题 + 顺带修的两个既有缺陷

### 一、苏格拉底对话「点了没反应」（三个叠加的缺陷）

用户反馈：① 无法正常进入对话页 ② 历史记录里仍有输入框 ③ 历史回看没有退出按钮。

**缺陷 1：建对话直接 500 —— 提示词里压根没要求输出 JSON**

`ChatService` 建对话与回复追问时用的是 `SocraticPrompts.system()`，那是**纯角色描述，通篇没有一句要求输出 JSON**；而唯一写明 JSON 契约的 `turnSystem()` **全项目从未被引用**。模型自然回了自然语言，`AiJsonParser` 解析失败 → 500「AI 返回格式异常」。

修复三层加固：
- `turnSystem()` 改为「教学规则 + JSON 输出契约」，并在建对话/回复处改用它
- `AiGateway.chatJson` 接上 `response_format: {"type":"json_object"}` —— 此前 `chatTextDetail` 里 jsonMode 硬编码 `false`，**这道服务端约束一直是死代码**
- `AiJsonParser` 增brace 配平扫描（容忍 JSON 前后夹带说明文字），并在彻底失败时**把原文记入日志**——原先只抛「格式异常」，线上无从排查

**缺陷 2：失败的 AI 调用会留下孤儿会话，把用户永久卡死（最严重）**

原 `create()` 顺序是「插入会话 → 存用户消息 → 再调 AI」，且**方法没有事务**。AI 一旦失败，前两次 insert 已提交，留下一条 active 但没有追问的孤儿会话；而「同一用户只能有一个 active 对话」的校验随即生效 —— 用户**再也建不了新对话**，界面上表现为「点开始对话没反应」。

修复：改为**先调 AI、成功后才落库**，落库部分用 `TransactionTemplate` 包成一个事务。失败即什么都不留。

**缺陷 3：欢迎屏不渲染错误信息**

`{error && ...}` 只写在「对话中」分支，欢迎屏没有。所以创建被拒时界面毫无反馈。已在欢迎屏补上错误提示，并新增**「你有一个进行中的对话」卡片 + 继续入口**（查 `status=active` 的历史），避免用户卡在死胡同里。

**缺陷 4：对话页缺了用户自己那条气泡**（顺带发现）

后端建对话接口只返回 AI 的追问，前端 `messages: [data.message]` 就把用户提的需求丢了 —— 对话页看起来像凭空开始提问。已在 store 里本地补上用户消息。

### 二、按设计稿调整历史回看（用户明确要求）

历史记录**只展示记录与评分，不接受输入**。新增 `viewingHistory` 状态：
- 回看时底部输入区**整块隐藏**、实时评分面板不显示
- 头部换成只读样式（灰色 History 图标 + 「历史记录 · 只读回看（查看记录与评分，不支持继续输入）」）
- 头部提供 **[← 退出查看]**，点击回到欢迎屏
- 与 `loadHistory`（只读）区分出 `resumeChat`（继续追问，可输入），供欢迎屏的「继续这一条」使用

### 三、登录页印着不存在的测试账号

`test@example.com / 123456` 这条提示**在本地库与云端库都不存在**，照它输入必然失败；且与上方「以游客身份体验」按钮功能重叠，在公开登录页印可用凭据本身也是安全隐患。已移除。

### 四、密码框加眼睛图标（用户要求）

新增 `components/ui/password-input.tsx`，右侧眼睛切换明文/密文，应用于登录页、个人中心改密码（当前/新密码）、后台新建用户与重置密码，以及 API Key 输入框。
按钮必须写 `type="button"`，否则登录页的密码框在 `<form>` 里，**点眼睛会直接提交表单**。

### 五、验证情况

**苏格拉底对话（真实模型，本轮共 2 次 API 调用）**：
| 检查 | 结果 |
|---|---|
| 建对话返回 | **200**（原为 500），耗时 624ms |
| 进入对话页 | ✓ 用户需求气泡 + AI 真实追问「你希望这封邮件以什么角色身份撰写？」 |
| 需求文本出现次数 | 2（标题 + 气泡）——确认用户那条没丢 |
| 历史回看 | ✓ 有「退出查看」、标注只读、**无输入框**、无实时评分面板 |
| 退出查看 | ✓ 回到欢迎屏，输入框恢复 |
| 后端日志 | ERROR/Exception 计数 0 |

**密码框眼睛**（浏览器实测）：按钮存在且位于输入框右侧、垂直居中（中心 Y 坐标一致）；点击后 `type` 由 `password`→`text`，可切回；**value 不受切换影响**。

**登录**：直连后端与经 Vite 代理均返回 200；浏览器端到端登录成功进入 `/app`。

截图见 `D:\暑期ai学习\验证截图-20260913\`（第 14-22 张，共 22 张）。

### 六、追加修复：「对话几次后出现 AI 返回内容为空」

用户复现条件：苏格拉底对话进行若干轮后报「AI 返回内容为空，请重试」。

**排查过程（值得留档，因为前两个假设都是错的）**

1. 首次诊断直连 DeepSeek，返回正常 → 推出"推理模型把 max_tokens 吃光"的假设。**错**：响应里根本没有 `reasoning_content` 字段。
2. 于是怀疑高温采样抖动，把 JSON 调用的温度从 0.7 降到 0.2。**错得更彻底**：用同一条失败上下文在 0.2 下**连续 3 次全返回空白**（7 个空格）——不是抖动，是确定性失败。
3. 关键对照实验找到了真因：

| 结构 | 结果 |
|---|---|
| 原结构：`system` + 逐条传的历史消息（其中 assistant 轮次是**散文**） | `len=7` 空白 |
| 同一上下文但去掉 `response_format` | 返回**散文**（"你们计划玩几天？"），说明模型在模仿上下文风格 |
| 历史**压平成一条 user 消息** | `len=77` **合法 JSON** |

**根因**：历史上线的 assistant 轮次是散文（就是追问的问题本身）。模型会模仿上下文风格输出散文，而 `response_format=json_object` 又不允许散文 —— 约束解码器最终**只吐出一串空白**。所以**轮次越多越必然复现**，与温度无关。

**修复**：JSON 类调用（回合判断 / 生成优化结果）把对话历史**压平成一段文本放进 user 消息**，上下文里只剩 system + user，没有可模仿的散文轮次。见 `ChatService.buildTurnPrompt` / `buildCompletePrompt`。

**同批次一并修掉的关联缺陷**：`reply()` 与 `completeConversation()` 与 `create()` 有同样的毛病 —— 先落库再调 AI 且无事务，AI 失败会留下**没有回复的用户消息**。那条出问题的会话里堆积了 6 条（user 9 条 / assistant 3 条），上下文被污染成连续多条 user。已改为「先调 AI、成功后在事务里落库」。

**验证**：接着一条已有对话连续追问 4 轮，**全部 200**，第 4 轮正常完成并生成优化提示词；消息统计 `user=5 assistant=5`（无孤儿）；后端「返回内容为空 / 无法解析」计数 0，ERROR 计数 0。单元测试锁住两条：`ChatServiceTest.reply_aiFails_doesNotPersistUserMessage`、`AiGatewayTest.chatJson_retriesOnceWhenModelReturnsBlank`。

**诚实标注**：低温（0.2）**不是**这个问题的解，只是 JSON 抽取任务的常规选择；代码注释里已按事实写明，避免后人误以为降温度能解决空白。

### 七、追加修复：「对话最后一步白屏」

现象：苏格拉底对话走到**完成的那一轮**，整页变纯白。

**根因（一条链）**：
1. `ChatService.buildCompletedTurn` **从头到尾没有 `vo.setMessage(...)`** —— 完成的那一轮不返回消息，结果放在 `improvedPrompt` / `comparisonResult` 里
2. Jackson 未配置 `default-property-inclusion: non_null`，所以响应里是 `"message": null`
3. 前端类型 `SendMessageResponse.message: Message` **声明为必存在**，TypeScript 拦不住
4. store 里 `let nextMessages = [...get().messages, data.message]` —— **把 null 塞进了消息列表**
5. `ChatPage` 渲染时 `messages.map(msg => <MessageBubble key={msg.id} …>)` → 对 `null` 取 `.id` → **TypeError**
6. 全项目**没有任何 ErrorBoundary**，React 抛错即卸载整棵树 → **纯白页面，连提示都没有**

「最后一步」正好就是触发完成的那一轮，所以只有它白屏。

**修复**：
- 前端类型改为 `Message | null` 并注明何时为 null（让 TS 强制调用方判空）
- store 判空后再追加：`data.message ? [...] : get().messages`；`ChatPage` 渲染前再 `filter(Boolean)` 兜一层
- 后端 `ChatTurnVO.message` 补注释说明该契约
- **新增 `components/common/ErrorBoundary.tsx`**，挂两级：`App.tsx` 顶层兜底 + `WorkspacePage` 模块级（出错时左侧导航仍可用，切走再切回自动恢复）。这是本次最重要的加固 —— 以后任何渲染异常至少能看到原因并可重试，而不是一片空白

**验证**（真实模型跑到完成）：新开一条对话追问 3 轮后正常完成，页面存活、`ResultView` 正常渲染（优化后的提示词 + 5 条改进点 + 评分星星 + 右侧学习分 58/五要素 4/5 + 开始新对话），**运行时异常 0**，兜底面板未被触发。截图见 `23-苏格拉底-对话完成页.png`。

### 八、新增：配额「不限量」开关

用户要求联调期间先放开每日 AI 配额（`daily-limit` / `assistant-daily-limit`），需要时再加回去测试。

没有用「填个 99999」这种魔法数字，而是做成一个明确语义：

- `AiGateway.checkQuota`：**`limit <= 0` 表示不限量**，直接返回，连 Redis 计数器都不写（避免白跑一次写）
- `UpdateAiConfigReq`：两个配额的 `@Min(1)` 放宽为 `@Min(0)`
- 后台配置页的输入框 `min` 改为 0，提示补上「填 0 表示不限量（联调/压测用）」
- 监控页配额展示：为 0 时显示「不限」而不是「0/日」

**`application.yml` 的默认值仍保持 50 / 100** —— 那是给全新部署播种用的安全默认值，不应在生产变成「不限」；0 只写进当前这个库。

**验证**（真实调用）：
| 步骤 | 结果 |
|---|---|
| 把 `dailyLimit` 临时改回 1（当日已用 13） | 发起对话 → **429「今日 AI 调用次数已达上限」**，证明校验确实生效 |
| 两个配额改为 0 | 发起对话 → **200**，且 Redis 计数器**停在 13 未增长**，证明既不拦截也不计数 |

**当前状态**：两个配额均为 **0（不限量）**，存在数据库里、重启后仍生效。需要恢复时在「后台 → API 配置」把两个值改回 50 / 100 即可。

### 九、评分口径重构 + 打卡修复 + 隐藏每日挑战赛

#### 1. 学习分改为「系统综合评分」

**原状况**：学习地图的掌握度由**用户自己的满意度星级**（1-5 星）决定 ——
「ResultView 的 5 颗星 → POST /chat/{id}/rating → updateProgress(rating) → rating ≥ 3.5 即掌握」。
这与产品口径不符：学习分应当是系统按客观指标算出来的。

**新口径**（用户确认）：

```
综合评分(0-100) = 完整度 × 70% + 轮数效率 × 30%
  完整度   = 五要素得分合计（角色/任务/上下文/格式/约束，各 0-20）
  轮数效率 = 60 + (maxRounds - 已用轮数) / (maxRounds - 1) × 40
            → 用满 5 轮得 60，1 轮完成得 100
```

轮数效率**刻意给 60 的下限而不是 0**：AI 最多追问 5 轮，走完全程是正常路径，
按「越少越好」线性给分会让正常用户被系统性压低、够不到掌握阈值。

实现：
- 新增 `PromptScorer`（`modules/chat/scoring/`）—— 前端 `lib/promptScoring.ts` 的逐条移植。
  **两份实现是有意为之**：前端那份用于对话进行中的实时预估（每敲一个字都请求后端不现实），
  后端这份用于完成时的权威算分。两边都加了注释互相指向，改一处必须同步另一处。
- 完成对话时算分并落库：`conversations.score`（0-100）+ `element_scores`（五要素 JSON）
- `updateProgress` 改用综合评分判定掌握，阈值也换算到 0-100（见 V8 迁移）
- 用户星级**不再联动学习进度**，只存 `conversations.rating` 作体验反馈

**顺带修掉算法里一处不公平**：基线要素（原始提示词里就写明的）原先只记覆盖度、不记「具体度」，
每个封顶 10/20 分 —— 结果是「用户一上来就把五要素写全」只得约 50 分，反而**低于**被追问着补齐的人，
与「教人一次把提示词写好」的产品目标正好相反。已在 Java 与 TS 两侧同步修正基线也计具体度。

#### 2. 连续打卡（原为「不生效」）

**原状况**：`users.streak_days` **全项目只被读、从没被写过**（除注册时的 `setStreakDays(0)`），
所以永远显示 0。

**修复**：新增 `users.last_checkin_date`；完成任意一次对话即打卡 ——
昨天打过则 +1，否则重置为 1，今天已打过则不变。用「日期是否相邻」判断而非累加计数，
跨天或漏打都不会算错。

#### 3. 能力雷达接真实数据

原先 ProfilePage 的雷达**写死为 0**（注释还写着「后端接口待开发」）。
现改为由后端聚合：取用户最近 20 次对话的五要素分数求平均（单要素 0-20 换算到 0-100），
随 `GET /api/learning/progress` 的 `radar` 字段返回。维度 key/label 直接从落库 JSON 里取，
不在前端另立一套，避免与评分口径分叉。

同时把雷达维度从原来的（精确度/结构化/创造力/约束性/迭代力）**改为提示词五要素** ——
后者与学习地图、评分面板同源，用户能直接对上；前者当时没有任何数据来源。

#### 4. 隐藏每日挑战赛

按需求暂时下线：从侧栏导航移除，同时**移除「今日任务」里对应的挑战赛任务** ——
否则会留下一个点不进去的死任务。页面组件、`uiStore` 的 section 类型与 `WorkspacePage`
的分发都保留着，恢复时把导航项加回来即可。

#### 5. 验证情况（真实模型）

| 检查 | 结果 |
|---|---|
| V8 迁移 | ✓ 阈值换算为 入门70 / 进阶70 / 精通80 / 大师90 |
| 跑完一条关联知识点的对话 | 系统评分 **68**、星级为 NULL（未评） |
| 评分类字段落库 | ✓ `score=68`、`element_scores` 五要素齐全 |
| **掌握度判定** | 角色设定 `status=learning`、`best_rating=68`、阈值 70 → **68<70 未掌握，判定正确** |
| **打卡** | ✓ `streak_days=1`、`last_checkin_date=` 当天 |
| 能力雷达 | ✓ 5 个维度有真实分数（约束条件最低 50，与要素原始分 10/20 一致） |
| 统计口径 | ✓ 平均学习分 68.0/100、连续打卡 1 天、总对话数 3 |
| 侧栏 | ✓ 「每日挑战赛」已消失，今日任务由 1/4 变 1/3 |

截图：`24-学习地图-新评分口径.png`、`25-能力雷达-真实数据.png`。

**测试数据说明**：验证留下了一条已完成对话（`conv_9d396679e1d0`，评分 68）与一条知识点进度记录，
正好可以让你在页面上看到真实效果，故保留未删。

### 十、测试后清理

- 删除孤儿会话 `conv_3aeb4725e6ed`（本轮缺陷 2 的现场证据，只有 user 消息、无 AI 追问）
- 清空登录失败计数（`admin@spark.loacl` 拼错 3 次、`test@example.com` 1 次）
- API key 仅在环境变量中，配置未落库

---

## 迭代批次 · 2026-09-13（稳定版 Phase 3 · 用户管理 / API 配置 / 监控）

> 类型：`feat(admin)` 补齐原始需求中管理后台剩下的三块，并完成部署收尾与云端迁移
> 说明：**本批次完成后，最初提出的三项需求全部落地**

### 一、交付内容

**① 用户管理**（增删改查用户 id / 密码 / 头像 / 角色 / 状态）

| 接口 | 说明 |
|---|---|
| `GET /api/admin/users` | 分页 + 关键词（用户名/邮箱）+ 角色 + 状态筛选 |
| `POST /api/admin/users` | 新建（邮箱/用户名/密码/角色） |
| `PUT /api/admin/users/{id}` | 改邮箱与用户名 |
| `PUT /api/admin/users/{id}/role` | 改角色 |
| `PUT /api/admin/users/{id}/status` | 启用 / 禁用 |
| `POST /api/admin/users/{id}/password` | 重置密码 |
| `PUT /api/admin/users/{id}/avatar` | 直接设定头像（跳过审核的特权通道） |
| `DELETE /api/admin/users/{id}` | 软删 |

**拆成细粒度接口而非一个全量 PUT** —— 改角色、改状态、重置密码各自有完全不同的安全栏杆，混在一个接口里容易漏校验。
**用户 ID 是主键，只读**：没有提供任何修改入口，也不该有（改主键会打断所有外键引用）。

安全栏杆（全部实测拦截）：
- 不能改自己的角色 / 不能禁用自己 / 不能删除自己
- 不能移除或禁用**最后一个 active 管理员**（否则后台永久锁死，只能改库）
- **密码只能重置、无法查看**（BCrypt 哈希不可逆）；新密码由管理员自行输入，接口不回显
- 软删时混淆邮箱与用户名以释放唯一键；**硬删会撞外键**（conversations/works/templates/user_badges 都指向 users）
- 「直接设头像」只接受本服务上传路径前缀，拒绝任意外链
- 改角色/禁用会写 Redis 标记位 → **对方已有登录态在数秒内失效**，不必等 7 天 token 过期

**② API 配置**（入库 + 后台可改 + 实时生效）

新增 `ai_provider_config` 表（单行）。生效链路：**数据库 → 解密 → AiConfigHolder.refresh → 下一个 AI 请求立即用新值**，无需重启。

- apiKey 用 **AES-256-GCM** 加密存储（密钥取 `ai.config-secret`，缺失回落 `jwt.secret` 派生并告警）
- 接口**只返回脱敏值**（`sk-****d80a`），密钥只在 PUT 时单向上行
- 密钥三态：不传/传脱敏串 = 保持不变；传空串 = 清空（回落环境变量）；传新值 = 加密覆盖
- 返回里带 `apiKeySource`（数据库 / 环境变量）—— 排查「改了不生效」时一眼看出用的是哪份
- 新增 `enabled` 开关：关掉后所有 AI 接口直接 503，避免"关了还在烧额度"
- 「测试连接」走 `max_tokens=1` 的探测，把试连消耗压到最低；失败返回 `ok=false` 而非报错

**③ 后台监控**（四项全做）

| 项 | 实现 |
|---|---|
| AI 调用指标 | 新增 `ai_usage_daily` 表（按 用户×日期×场景 聚合）。**改造前用量只在 Redis，没有任何历史**，趋势图无从谈起 |
| 系统健康 | 把 HealthController 的探活抽成 `HealthService` 复用，并补 JVM/连接池指标；deepseek 状态改读运行时配置 |
| 用户与内容统计 | 注册/活跃/会话/作品总量与按天趋势，图表用 recharts |
| 日志查看 | 新增 `system_logs` 表 + RequestLogFilter 改造 + 分页筛选；配 30 天保留清理任务 |

- 用量落库走 `AiUsageRecorder`：**有界队列 + 单消费线程**，队列满则丢弃并告警 —— 统计写入绝不拖慢 AI 调用
- 日志只记 **status ≥ 400 或耗时 ≥ 1000ms**；全量入库会把数据库写成热点
- `userId` 从 **MDC** 取（RequestLogFilter 跑在 Security 链之外，finally 时 SecurityContext 已清空）
- 日志表用 `BIGINT AUTO_INCREMENT` 主键，**刻意偏离项目的 `{前缀}_{uuid}` 约定**：写入量最大、无需对外暴露前缀，随机字符串主键会造成页分裂

**④ 部署收尾**
- nginx：`/api/` 加 **`proxy_buffering off`**（否则生产 SSE 逐字输出退化成整段蹦出）+ 超时提到 300s；新增 `/uploads/` 代理；**移除 `/actuator/` 转发**（prometheus 已下线，其余端点要 ADMIN，转发只是扩大攻击面）
- **`location ^~ /uploads/` 必须用 `^~`** —— 否则会被下面的静态资源正则抢先匹配，从 nginx 本地目录找文件而 404
- docker-compose：backend 加 `UPLOAD_DIR` + `upload-data` volume（不挂的话重建容器会丢光已通过的头像），并补 `AI_CONFIG_SECRET`

### 二、验证情况（真实模型 + 真实浏览器）

**真实 DeepSeek API 验证（共 4 次调用，额度受控）**

| # | 用途 | 结果 |
|---|---|---|
| 1 | 试连（max_tokens=1） | ok，971ms；服务端回报实际命中 `deepseek-flash` |
| 2 | 真实助手对话（流式） | 179 个 delta 帧跨 1.18s 到达；回复 306 字 |
| 3 | 改 model 后试连 | 成功 |
| 4 | 用**不存在的模型名**试连 | **400 且错误里出现我写入的名字** → 证明请求确实用了库里的配置，热更新成立。此调用失败，零 token 消耗 |

**引导式红线首次得到真实验证**（这是 Phase 1 遗留的未验证项）：故意索要成品「直接给我写一个完整的、能用的提示词」，模型回答：

> "可以，但先确认一件事：**你要它做什么？** 提示词没有'通用万能版'…直接给你一版，多半你用不上。
> 1. **任务**：你想让 AI 帮你做什么？ 2. **对象/场景**：谁在用？
> 当然，如果你就是想要一版提示词模板，只要回我一句'给我一版通用的'，我会给，并注明每一句为什么这么写。"

**拒绝直接给成品、改用追问引导**，正是 system prompt 要求的红线，且留了合理退路。

**后台安全栏杆实测**：改自己角色 / 禁用自己 / 删除自己 → 400；降级唯一管理员 → 400；重复邮箱 → 400；禁用后登录 → **403「账号已被禁用」**；外链头像 → 400；软删后邮箱用户名被混淆、登录 400；普通用户访问 → 403。

**验证核对**：监控页显示 AI 调用 1 次 / 成功率 100% / 平均耗时 1674ms / 407 tokens —— 与真实调用一致，说明用量确实落库。

**其他**：后端 37 单测全绿；前端 `tsc --noEmit` 与 `vite build` 通过；四个后台页面截图存档。

### 三、云端库迁移

云端 `162.251.93.7` 已从 V4 升到 **V7**（V5 头像审核 / V6 AI 配置 / V7 用量与日志，共 16 张表）。
迁移前 `mysqldump` 备份至 `D:\暑期ai学习\db-backups\cloud-prompt_learning_platform-20260913-165013-before-v5v7.sql`。
**迁移时刻意不传 `DEEPSEEK_API_KEY`** —— 播种写入的是 NULL，`api_key_cipher` 为空、运行时读环境变量，**密钥没有被写进云端库**。

### 四、测试后的状态恢复

按要求用完 API key 后已恢复测试前状态：清空库里加密的密钥（回落环境变量）、model 与各项限额复位默认。当前 `apiKeySource=环境变量`、`apiKeyConfigured=true`，功能不受影响。

### 五、遗留与下一步

| 项 | 说明 | 归属 |
|----|------|------|
| 前端 mock | PRODUCT.md 要求「上线前全量清除 mock」——灵感广场/学习地图/徽章/挑战赛排行榜仍是本地种子数据（后端接口未实现） | 后续 |
| 后端单测 | 用户管理、API 配置、监控目前只有 e2e 覆盖；LocalFileStorage 的魔数/尺寸/路径穿越分支适合补单测 | 后端 |
| 试连不计量 | 「测试连接」走 AiHttpClient.probe 直连，**不经过 AiGateway**：不计入配额、不落 ai_usage_daily。这是有意为之（管理员诊断不该占用户配额），但意味着它可以被反复调用，必要时加限流 | 后端 |
| 日志表清理 | 已配 30 天保留；若线上错误风暴，队列会丢弃而非堆积，但库容量仍需观察 | 运维 |
| 提交 | 三个批次的改动均未提交；`全局AI助手方案-v1.md`、`版本迭代记录.md` 仍未入库 | 版本管理 |

### 附：本批次新增文件（后端 20 + 前端 7）

```
后端 · 迁移
db/migration/{V6__ai_provider_config,V7__ai_usage_daily_and_system_logs}.sql

后端 · 公共
common/util/CryptoUtil.java

后端 · 管理端
modules/admin/entity/{AiProviderConfig,AiUsageDaily,SystemLog}.java
modules/admin/mapper/{AiProviderConfigMapper,AiUsageMapper,SystemLogMapper,AdminUserQueryMapper,MonitorQueryMapper}.java
modules/admin/dto/{CreateUserReq,UpdateUserReq,UpdateUserRoleReq,UpdateUserStatusReq,
                   ResetPasswordReq,SetAvatarReq,UpdateAiConfigReq}.java
modules/admin/vo/{AdminUserVO,AiConfigVO,AiConfigTestVO,SystemLogVO}.java
modules/admin/service/{AdminUserService,AiConfigService,MonitorService,LogQueryService,
                       QueuedRecorder,AiUsageRecorder,SystemLogRecorder}.java
modules/admin/controller/{AdminUserController,AdminConfigController,
                          AdminMonitorController,AdminLogController}.java
modules/admin/config/AdminAsyncConfig.java      （含日志保留清理任务）

后端 · 复用改造
modules/health/service/HealthService.java       （从 HealthController 抽出）
modules/health/HealthController.java            （改为委托）
common/filter/RequestLogFilter.java             （+错误与慢请求落库）
ai/AiHttpClient.java                            （+probe 探测）
ai/AiGateway.java                               （+enabled 开关、+latency Timer）

前端 · 新增
src/components/admin/{AdminPageHeader,StatCard,Banner}.tsx
src/pages/admin/{AdminUsersPage,AdminConfigPage,AdminMonitorPage,AdminLogsPage}.tsx

前端 · 改动
src/services/adminService.ts  src/types/index.ts
src/App.tsx  src/components/layout/{AdminSidebar,AdminTopBar,Sidebar}.tsx

部署
nginx/nginx.conf          （SSE 不缓冲 / uploads 代理 / 移除 actuator 转发）
docker-compose.yml        （upload volume + AI_CONFIG_SECRET）
```

---

## 迭代批次 · 2026-09-13（稳定版 Phase 2 · 头像审核 + 管理后台骨架）

> 类型：`feat(profile/admin)` 头像上传与三态审核闭环 + 管理后台骨架
> 说明：原计划把管理后台整体放在 Phase 3，实际把**骨架**（布局 / 路由 / 通用组件）提前到本批次
> —— 否则头像审核无法端到端验证。Phase 3 变为在现有骨架上添加页面，总范围不变。

### 一、交付内容

**数据模型**（`V5__avatar_review.sql`）—— 双槽位设计：

| 列 | 含义 |
|---|---|
| `users.avatar` | 当前**对外可见**的头像，只在审核通过后写入 |
| `users.avatar_status` | none / pending / approved / rejected |
| `users.avatar_pending_url` | 待审候选，审核结束后清空 |
| `users.avatar_reject_reason` | 最近一次驳回理由，供用户端展示 |

状态机：`none →(上传) pending →(通过) approved` / `→(驳回·理由必填) rejected →(重新上传) pending`。
**驳回时 `avatar` 保持原值不变** —— 用户不会因一次驳回丢掉已有头像；从未设置过的回落默认占位。
每次提交往 `avatar_reviews` 插新行（不覆盖），保留完整留痕。

**后端**
| 接口 | 说明 |
|---|---|
| `GET /api/profile` | 个人资料（含 avatarStatus / 驳回理由） |
| `PUT /api/profile` | 改用户名（**不含 avatar**，否则绕过审核） |
| `PUT /api/profile/password` | 改密码（校验旧密码） |
| `POST /api/profile/avatar` | 提交头像审核（multipart） |
| `GET /api/profile/avatar` | 查询头像状态 |
| `GET /api/admin/avatars` | 审核列表（可按状态筛） |
| `GET /api/admin/avatars/stats` | 各状态计数 |
| `POST /api/admin/avatars/{id}/approve` | 通过 |
| `POST /api/admin/avatars/{id}/reject` | 驳回（理由 2-200 字必填） |

**上传安全**（`common/storage/LocalFileStorage`）四道关，缺一不可：
1. **魔数嗅探**判断真实格式 —— Content-Type 与扩展名都是客户端可伪造的
2. **解码前只读图片头拿尺寸** —— 防「2MB 的 PNG 解出上亿像素」把内存打爆
3. **ImageIO 重新编码** —— 剥离 EXIF，并让伪装成图片的 polyglot 文件解码失败
4. **文件名完全由服务端生成** —— 客户端文件名一律丢弃，杜绝路径穿越

**前端**
- `components/common/UserAvatar.tsx` —— 有已通过头像显示图片，否则回落首字母占位
- `pages/profile/components/AccountSettings.tsx` —— 头像上传（canvas 预缩到 512px）+ 三态提示 + 改用户名 + 改密码
- 管理后台骨架：`AdminGuard`（非管理员渲染 403 面板而非静默跳转）、`AdminLayout` / `AdminSidebar` / `AdminTopBar`、`/admin/*` 真路由
- 通用组件：`ui/modal`（从 works 的私有 ModalShell 提取）、`ui/table`、`ui/pagination`
- `components/layout/BackgroundLayer` —— 抽出后 AppLayout 与 AdminLayout 共用，避免视觉分叉

### 二、修的五个既有缺陷（全部由端到端测试暴露）

**1. axios 不会为 FormData 自动改写 Content-Type（我自己的错误假设）**

上传头像返回 500 `HttpMediaTypeNotSupportedException: Content-Type 'application/json' is not supported`。
我在 `profileService` 里写了「axios 会自动处理」的注释，**这是错的**：实例上默认的 `Content-Type: application/json` 会被原样发出，而端点声明了 `consumes = multipart/form-data`。
手动写 `multipart/form-data` 同样不行（缺 boundary）。唯一正确做法是在请求拦截器里检测到 FormData 时**删掉这个头**，交给浏览器生成。

**2. `vite.config.js` 遮蔽了 `vite.config.ts`（Vite 配置完全不生效）**

项目里同时存在两者。Vite 解析配置时 **`.js` 优先于 `.ts`**，而 `.js` 是旧版编译产物且被 git 跟踪 —— 于是改了 `.ts` 毫无效果（表现为 `/uploads` 代理配了没反应，图片 404）。
已 `git rm` 两个产物并加入 `.gitignore`（连同 `*.tsbuildinfo`）。

**3. 限流 key 缺少接口维度 → 跨端点串台**

`buildKey` 返回 `rl:{type}:{身份}`，**不含具体接口**。所有 `type="user"` 的端点共用一个计数器，但阈值各取本方法的注解：用户传了几次头像（5/分）后，**改密码（同样 5/分）会被误判超限 429**。窗口时长也由最先创建 key 的注解决定。
已改为 `rl:{type}:{身份}:{Controller.method}`。

**4. 客户端错误被统一兜底成 500**

`GlobalExceptionHandler` 的 `catch (Exception)` 把客户端错误误报为服务端故障。已补：
- `NoResourceFoundException` → **404**（路由不存在）
- `HttpRequestMethodNotSupportedException` → **405**
- `HttpMessageNotReadableException` → **400**（含请求体不是合法 UTF-8）
- `HttpMediaTypeNotSupportedException` → **415**
- `MaxUploadSizeExceededException` → **400**

**5. `AvatarImage` 全项目从未被使用 → 真实头像永远不显示**

Sidebar / TopBar 只渲染 `AvatarFallback`，所以即使 `users.avatar` 有值也从不会显示。已接入 `UserAvatar` 修正。
顺带补上 `AuthUser` 类型里一直缺失的 `avatar` 字段。

### 三、验证情况（全部实测）

**后端 API 全流程**（含边界）：
| 用例 | 结果 |
|---|---|
| 伪装成 PNG 的文本 | 400（魔数嗅探拦下） |
| 3MB 大文件 | 400 |
| 合法 PNG | 200 → pending，且 **users.avatar 仍为 null**（不提前展示） |
| 待审期间重复上传 | 400 |
| 普通用户访问 `/api/admin/**` | 403 |
| 空驳回理由 | 400 |
| **驳回后** | 用户端 `avatarStatus=rejected` + 理由可见，**`avatar` 仍为 null（未丢）**，且被驳回的文件已从磁盘删除（HTTP 404） |
| **通过后** | `avatar` 更新、文件可访问（200） |
| 重复审核同一条 | 400（幂等保护） |
| 改密码：旧密码错 | 400 |
| 限流隔离 | 头像端点打到 429 后，改密码仍 200 且头像端点仍 429（计数确实隔离） |

**真实浏览器全流程**（headless Chrome + CDP，含 `DOM.setFileInputFiles` 真实选文件）：
用户登录 → 能力雷达 → 账号设置选图上传 → 页面出现「审核中」→ 切换管理员 → `/admin/avatars` 待审列表 → 填写理由驳回 → 成功提示 → 切回用户 → **看到「已驳回」徽标 + 驳回理由，头像显示为默认占位**。
截图核对：管理后台视觉与工作台一致（品牌靛蓝 / 白卡 / 衬线标题 / 光斑背景），筛选 chip 带计数、表格缩略图正常、分页齐全。

**其他**：后端 37 单测全绿；前端 `tsc --noEmit` 与 `vite build` 通过。

### 四、遗留与下一步

| 项 | 说明 | 归属 |
|----|------|------|
| Phase 3 | 用户在 Phase 3 要的用户管理 / API 配置 / 四项监控 —— 现在可直接在已有 AdminSidebar 上加导航项与页面 | 前端为主 |
| 全局 Toast | 本批次用页面内联提示与局部 toast，未建全局 toast 系统 | 前端 |
| 后端单测 | 头像上传与审核流程目前只有 e2e 覆盖，未补单元测试（LocalFileStorage 的魔数/尺寸/路径穿越分支适合单测） | 后端 |
| WebP | `ImageIO` 无 WebP reader，当前只支持 JPEG/PNG；需要时加 `webp-imageio` 依赖 | 后端 |
| 孤儿文件清理 | 用户记录被删除时其上传统头像文件不会自动清理，需要时加定时任务 | 后端 |
| 云端迁移 | 本批次新增 V5，云端库尚未执行（本地已跑通） | 部署 |

### 附：相关文件清单（本批次，共 21 改 + 24 增）

```
后端 · 迁移与存储
project/ai-platform/src/main/resources/db/migration/V5__avatar_review.sql   （新增）
project/ai-platform/src/main/java/com/aiplatform/common/storage/LocalFileStorage.java （新增）
project/ai-platform/src/main/java/com/aiplatform/config/WebMvcConfig.java   （新增，静态资源映射）

后端 · profile 模块（此前目录建好但一行代码都没有）
project/ai-platform/src/main/java/com/aiplatform/modules/profile/
  controller/ProfileController.java        service/ProfileService.java
  entity/AvatarReview.java                 mapper/AvatarReviewMapper.java
  dto/{UpdateProfileReq,ChangePasswordReq}.java
  vo/{ProfileVO,AvatarStatusVO}.java

后端 · admin 模块（新增骨架）
project/ai-platform/src/main/java/com/aiplatform/modules/admin/
  controller/AdminAvatarController.java    service/AvatarReviewService.java
  dto/RejectAvatarReq.java                 vo/AvatarReviewVO.java

后端 · 改动
config/RateLimitAspect.java            （限流 key 加接口维度）
config/SecurityConfig.java             （ASYNC/ERROR 派发放行 —— Phase 1）
common/GlobalExceptionHandler.java     （+400/404/405/415）
modules/auth/entity/User.java          （+3 个头像字段）
application.yml                        （+app.upload.*）
.gitignore（根与后端）                  （忽略 uploads/）

前端 · 新增
src/components/common/UserAvatar.tsx          src/components/common/AdminGuard.tsx
src/components/ui/{modal,table,pagination}.tsx
src/components/layout/{AdminLayout,AdminSidebar,AdminTopBar,BackgroundLayer}.tsx
src/pages/admin/AdminAvatarsPage.tsx
src/pages/profile/components/AccountSettings.tsx
src/services/adminService.ts

前端 · 改动
src/App.tsx                              （+/admin/* 路由）
src/components/layout/{AppLayout,Sidebar,TopBar}.tsx
src/pages/profile/ProfilePage.tsx        （+账号设置区块 + authStore 头像同步）
src/pages/auth/LoginPage.tsx             （demo 用户补 avatar）
src/pages/works/components/PromptModals.tsx （ModalShell 改为引用提取出的 Modal）
src/services/{apiClient,profileService}.ts
src/{index.css,types/index.ts,lib/mockData.ts}
vite.config.ts                           （+/uploads 代理）
vite.config.{js,d.ts}                    （删除 —— 它们遮蔽了 vite.config.ts）
.gitignore                               （忽略 vite.config 产物 / *.tsbuildinfo）
```

---

> 类型：`feat(assistant)` 全站 AI 助手「提问导师」—— 悬浮气泡 + 右侧抽屉 + SSE 逐字输出
> 依据：`全局AI助手方案-v1.md`（方案有三处缺陷，本批次按修正后的设计实现，见下）

### 一、交付内容

**后端**（新增 `modules/assistant`）
| 接口 | 说明 |
|---|---|
| `POST /api/assistant/conversations` | 新建会话（可不带 body） |
| `GET /api/assistant/conversations` | 历史列表（分页，含消息数） |
| `GET /api/assistant/conversations/{id}` | 会话详情（回看） |
| `DELETE /api/assistant/conversations/{id}` | 删除（前端不暴露入口，留给后台审计） |
| `POST /api/assistant/chat` | 非流式发消息（降级 / 测试用） |
| `POST /api/assistant/chat/stream` | **SSE 流式**发消息 |

**SSE 事件协议**（本项目**唯一不使用 `Result<T>` 信封**的接口）：
```
event: meta   data: {"conversationId":"conv_x"}
event: delta  data: {"content":"你"}
event: done   data: {"conversationId":"...","messageId":"...","usage":{...}}
event: error  data: {"code":500,"error":"AI 服务暂时不可用，请稍后重试"}
```
`emitter` 创建**之前**的失败（400/403/404/429）仍返回普通 JSON，前端可据 HTTP 状态区分。

**前端**
- `lib/sseClient.ts` — fetch + ReadableStream 手写 SSE 解析（详见「三、方案缺陷修正」）
- `store/assistantStore.ts` — 会话/消息/流式缓冲；增量按 ~50ms 节流批量 flush，避免每 token 一次 setState 导致整列表重渲染
- `components/assistant/{FloatingBubble,AssistantDrawer,AssistantHistoryPanel}.tsx` — 右下角气泡 + 抽屉（套 `HistoryDrawer` 骨架）
- `MessageBubble` 新增 `streaming` 属性：流式期间用纯文本 + 闪烁光标渲染，`done` 后切回 markdown —— 否则每 token 重解析 markdown 是 O(n²)

**关键工程决策**
- **上下文窗口**：取最近 20 条消息、累计 ≤12000 字符，超出从最旧开始丢（方案要求防无限膨胀；「超长摘要」留待后续）
- **配额隔离**：助手走 `ai:usage:assistant:{user}:{date}`，独立上限 `ai.assistant-daily-limit`（默认 100），不与苏格拉底抢每日 50 次
- **会话标题**：首条用户消息前 30 字自动回填，无需用户命名
- **中断处理**：客户端断开时把已下发的增量落库，避免会话里留下「有提问、无回复」导致下一轮上下文出现连续两条 user 消息
- **中文输入法**：Enter 发送需判 `isComposing`，否则用回车确认候选词会被误当作发送

### 二、修掉的两个真实缺陷（均为端到端测试才暴露）

**1. SseEmitter + Spring Security 的异步派发 403（严重）**

流式结束后，容器会发起一次 **ASYNC 派发**回到过滤链。`JwtAuthenticationFilter` 继承 `OncePerRequestFilter`，**默认跳过异步派发** → SecurityContext 为空 → `anyRequest().authenticated()` 判 **403**。表现为日志里一条 `AccessDeniedException`，客户端（经 nginx / Vite 代理时）**一直挂到超时**。

修复：`SecurityConfig` 加 `.dispatcherTypeMatchers(DispatcherType.ASYNC, DispatcherType.ERROR).permitAll()` —— ASYNC/ERROR 是容器内部回调，不是新的客户端请求，原始 REQUEST 派发仍然鉴权。

**2. 请求体格式错误被报成 500**

`HttpMessageNotReadableException`（含请求体不是合法 UTF-8 的情况）原先走兜底 `Exception` 分支返回 500，属于把客户端错误误报成服务端故障。已补 400。

（Phase 0 已修同类问题：路由不存在 → 404、方法不匹配 → 405、上传超限 → 400。）

### 三、对方案文档的修正（实现时按修正版落地，文档待回写）

| 方案原文 | 问题 | 实际做法 |
|---|---|---|
| 「SSE 逐字渲染 ← **EventSource**」 | `EventSource` **无法携带 `Authorization` 头且只支持 GET**，与全链路 Bearer 鉴权 + POST body 直接冲突 | 用 `fetch` + `ReadableStream` 手写解析；401 的全局登出逻辑在 sseClient 内单独复制一份（绕过了 axios 拦截器） |
| 接口路径 `/api/assistant/chat` 与 `/api/assistant/stream` 两处不一致 | 文档内部矛盾 | 统一为 `/api/assistant/chat`（非流式）与 `/api/assistant/chat/stream`（流式） |
| 「挂 AppLayout = 全站可唤起」 | `AppLayout` 只包 `/app`，覆盖不到 `/landing` 与 `/login` | 按锁定决策「助手仅登录用户可用」处理：气泡只挂登录后区域，`/landing`、`/login` 不出现 |
| 未提及 `ChatService` 无类型过滤 | 助手长驻 active 会话会**阻塞用户创建苏格拉底对话**并污染历史/徽章/均分统计 | Phase 0 已修（四处补 `conversation_type='socratic'` 过滤） |
| 未提及 `AiGateway` 熔断边界 | 原写法 `retry` 包住整流，用于流式会产生**重复 token** | Phase 0 已拆：熔断与重试**只作用于建连**，拿到响应头后不再重试 |

### 四、验证情况（全部实测）

**测试手段**：新增开发工具 `project/ai-platform/dev-tools/fake-sse-upstream.py` —— 本地假 OpenAI 兼容上游，SSE 每帧间隔 0.3s，**不消耗真实 API 额度**，且能用帧到达时间证明「增量是逐帧转发的」。

```bash
python dev-tools/fake-sse-upstream.py            # 监听 127.0.0.1:9099
DEEPSEEK_BASE_URL=http://127.0.0.1:9099 DEEPSEEK_API_KEY=sk-fake java -jar target/ai-platform.jar
```

| 验证项 | 结果 |
|---|---|
| 直连后端 SSE 帧间隔 | 289 / 300 / 301 / 300 / 301 ms —— 与假上游 0.3s 完全吻合（非一次性吐出） |
| **经 Vite 代理** SSE | 1.91s 完成、事件序列 `meta → 6×delta → done`、帧间隔 291/300/301/299/301 ms，**代理未缓冲** |
| 心跳注释帧 | 上游的 `: ping` 被后端正确吞掉，未下发给前端；后端每 15s 自己发一帧保活 |
| 会话标题回填 | 自动填为「什么是五要素？」 |
| **类型隔离** | 助手会话建好后，`/api/chat/history` 的 `total = 0` —— 未污染苏格拉底 |
| 错误路径 | 不存在会话 404、空白内容 400、非法字节请求体 400、未登录 401 |
| 非流式 `/chat` | 200，落库 `messageType=chat` |
| **真实浏览器 UI**（headless Chrome + CDP） | 登录 → 气泡出现 → 打开抽屉 → 点引导问题 → **流式中途截图只显示「你好，这是一段流式」+ 闪烁光标，3 秒后才是完整的「你好，这是一段流式回复测试」** → 证明 UI 逐 token 增量渲染；流式期间发送按钮变停止按钮 |
| 修复后日志 | `AccessDenied` / `ERROR` / `Exception` 计数为 **0** |
| 后端单测 | 37 个全绿；前端 `tsc --noEmit` 与 `vite build` 通过 |

### 五、遗留与下一步

| 项 | 说明 | 归属 |
|----|------|------|
| nginx | `/api/` 仍需加 `proxy_buffering off` 否则**生产**环境 SSE 无逐字效果（本地 Vite 代理已验证不缓冲） | Phase 4 |
| 上下文摘要 | 当前只做窗口截断，方案要求的「超长摘要」未做（需额外一次 AI 调用、计入配额） | 后续 |
| 停止后的续接 | 用户点「停止」后不能「继续生成」，需重新提问 | 后续 |
| 助手接徽章 | 方案开放问题 3，原建议留 P1 | 后续 |
| 文档回写 | `全局AI助手方案-v1.md` 需按上表修正后入库 | 文档 |

### 附：相关文件清单

```
后端（新增）
project/ai-platform/src/main/java/com/aiplatform/modules/assistant/
  controller/AssistantController.java        service/AssistantService.java
  prompt/AssistantPrompts.java               config/AssistantAsyncConfig.java
  mapper/AssistantQueryMapper.java           dto/{AssistantChatReq,CreateAssistantConvReq}.java
  vo/{AssistantConversationVO,AssistantConvDetailVO}.java
project/ai-platform/dev-tools/fake-sse-upstream.py   （本地假上游，验证用）

后端（改动）
project/ai-platform/src/main/java/com/aiplatform/config/SecurityConfig.java          （ASYNC/ERROR 派发放行）
project/ai-platform/src/main/java/com/aiplatform/common/GlobalExceptionHandler.java  （+400）

前端（新增）
project/frontend/src/lib/sseClient.ts
project/frontend/src/services/assistantService.ts
project/frontend/src/store/assistantStore.ts
project/frontend/src/components/assistant/{FloatingBubble,AssistantDrawer,AssistantHistoryPanel}.tsx

前端（改动）
project/frontend/src/components/chat/MessageBubble.tsx   （+streaming 属性）
project/frontend/src/components/layout/AppLayout.tsx     （挂载气泡与抽屉）
project/frontend/src/types/index.ts                      （助手类型）
```

---

## 迭代批次 · 2026-09-13（稳定版 Phase 0 · 地基）

> 类型：`feat(backend/frontend)` 首个稳定版的地基改造 —— 数据库迁移框架、权限体系、AI 网关重构
> 下游：Phase 1（AI 小助手）· Phase 2（头像审核）· Phase 3（管理后台）均依赖本批次

### 〇、种子管理员（⚠️ 必读）

迁移 `V3__seed_admin_user.sql` 会插入一个管理员账号，用于首次进入管理后台：

| 项 | 值 |
|---|---|
| 邮箱 | `admin@spark.local` |
| 用户名 | `admin` |
| 初始密码 | `Spark@Admin2026` |

**该密码明文写在迁移文件里且仓库公开，登录后必须立即修改**（个人中心 → 修改密码，Phase 2 提供；在那之前可由运维直接改库）。
迁移用 `WHERE NOT EXISTS` 守卫，若邮箱已被占用则跳过而不中断。

### 一、变更总览

| 项 | 内容 | 影响范围 |
|---|---|---|
| 数据库迁移 | 引入 Flyway，建立 V1 基线 + V2/V3/V4 增量迁移 | 后端 / 全部环境 |
| 权限体系 | users 加 `role`/`status`，JWT 带 role claim，`/api/admin/**` 限 ADMIN | 后端全站认证链路 |
| 会话类型隔离 | conversations 加 `conversation_type`，助手与苏格拉底互不污染 | 后端 chat 模块 |
| AI 网关重构 | 动态配置持有者 + 流式接口 + 分场景配额 + Redis 故障降级 | 后端 ai 模块 |
| 前端必修 | 3 个存量 bug、`landing.html` 构建缺失、登录态持久化迁移 | 前端工程 |

### 二、数据库迁移（Flyway）

引入原因：`db/init.sql` 挂在 `docker-entrypoint-initdb.d`，**仅容器首次启动执行一次**，对已有库完全无效；此前所有结构变更只能手工 `ALTER`，各环境必然分叉。

| 迁移 | 内容 |
|---|---|
| `V1__baseline_schema.sql` | 现有 `init.sql` 副本（去掉 `CREATE DATABASE`/`USE`），全部 `IF NOT EXISTS`，对已有库为空操作 |
| `V2__users_role_status.sql` | users 加 `role`(默认 `USER`)、`status`(默认 `active`)、`last_login_at` + 索引 |
| `V3__seed_admin_user.sql` | 种子管理员（见上） |
| `V4__conversations_type_and_long_text.sql` | conversations 加 `conversation_type`(默认 `socratic`)/`title`、`original_prompt` 放开 NULL；messages `content` 扩 TEXT、`message_type` 加默认值 |

`baseline-on-migrate: true` + `baseline-version: 0` → 已有库自动记一条 version=0 基线后跳过 V1 前的历史。
**V1 在已有库上会打出 11 条 `Table already exists` WARNING，属预期行为，不影响成败。**

### 三、权限体系

- `users.role` 贯通：`JwtUtil.generate(userId, role)` 写入 `role` claim；`JwtAuthenticationFilter` 据此注入 `ROLE_ADMIN`/`ROLE_USER`（改造前权限集合硬编码为 `List.of()`）。
- `SecurityConfig` 新增 `@EnableMethodSecurity`、`/api/admin/**` → `hasRole("ADMIN")`；`/actuator/**` 由 `permitAll` 收紧为仅 `health`/`info` 公开，其余限 ADMIN。
- **`/actuator/prometheus` 端点已从 exposure 移除**（项目内无 Prometheus 在抓取），后台监控改为直读 `MeterRegistry`。
- 登录新增 `status` 校验（`disabled` → 403、`deleted` → 400 且不暴露账号已删），并记录 `last_login_at`。
- **改角色/禁用秒级生效**：管理员操作写 Redis 标记位 `user:role:{id}` / `user:disabled:{id}`，过滤器优先读它，不必等旧 token 的 7 天有效期。Redis 故障时 fail-open（按未禁用处理），登录查库是第二道闸。

### 四、会话类型隔离（若无此改动，助手会直接锁死苏格拉底）

助手复用同一张 `conversations` 表，其会话常驻 `active`。`ChatService` 原先四处查询只按 `userId` 过滤，会导致：用户**永远无法再创建苏格拉底对话**、历史列表被污染、`conversation_count` 徽章被灌水、平均分统计窗口被挤占。四处均补 `conversation_type='socratic'`，并新增 `requireOwnedSocratic` 守卫拒绝跨类型误调。

### 五、AI 网关重构

- `AiConfigHolder`（`AtomicReference<AiRuntimeConfig>`）持有运行时配置快照，为 Phase 3 的「后台改配置热更新」铺路。`AiHttpClient` 不再把 `baseUrl`/`apiKey` 烘焙进构造器。
- **新增 `AiGateway.chatStream(...)`**：熔断与重试**只作用于建连**（拿到响应头后不再重试，否则会重复输出 token）；SSE 按行解析 `data:` 帧，容忍心跳注释与坏帧，`[DONE]` 后停止消费。
- **配额按场景分账**：key 改为 `ai:usage:{scope}:{userId}:{date}`，新增 `ai.assistant-daily-limit`（默认 100），助手不再与苏格拉底抢每日 50 次。Redis 故障时 fail-open。
- 新增 `AiUsageListener` 钩子（Phase 3 落库用），当前为 NOOP。

### 六、前端必修项

- **`landing.html` 生产 404**：原先位于项目根目录、无 `public/` 目录，`npm run build` 不会产出它。已移入 `public/landing.html`（Vite 原样拷贝到产物根）。**这替代了此前迭代记录里"保留 vite.config 多页入口"的方案，更简单且不引入 HTML 解析风险。**
- **`.wb-btn-primary:hover` 是旧版陶土橙 `#b4654a`** → 改为品牌靛蓝 hover `#3D31D6`。
- **粒子背景是橙色**（`204,120,92` / `232,165,90`）→ 改为靛蓝 `75,63,227` / `122,111,240`。
- `index.css` 与 `App.tsx` 中残留的暗色/陶土注释与实现矛盾，已改写为准确的浅色靛蓝描述。
- `authStore` 加 `version: 2` + `migrate`：旧持久化数据的 `user` 无 `role`、旧 token 无 role claim，直接清空强制重登（唯一干净解）。
- 类型补齐：`AuthUser.role`、`MessageType` 加 `"chat"`、`HealthStatus.services` 补 `redis`。

### 七、顺手修的既有缺陷

`GlobalExceptionHandler` 的兜底 `Exception` 分支把**客户端错误误报为 500**，影响全站 API。已补：路由不存在 → 404、方法不匹配 → 405、上传超限 → 400。

### 八、验证情况

- **后端 37 个单测全绿**（新增 `AiGatewayTest` 5 个，覆盖 SSE 增量拼接、坏帧容忍、`[DONE]` 截断、配额分账、非 2xx 转 500）。
- **本地库**：5 条迁移全部 `success=1`；权限端到端实测——普通用户访问 `/api/admin/**` 得 **403**（改造前该分支永远走不到）、普通用户访问普通接口仍 200、篡改签名 401。
- **云端库 `162.251.93.7` 已迁移**：迁移前先 `mysqldump` 备份至 `D:\暑期ai学习\db-backups\cloud-prompt_learning_platform-20260913-155035.sql`（仓库外，避免 PII 入库）；迁移前核对 schema 与 `init.sql` **逐列一致、无漂移**；迁移后 5 条记录 `success=1`，管理员登录返回 `role=ADMIN`。
- **前端**：`tsc --noEmit` 与 `vite build` 通过，`dist-build/landing.html` 已产出。

### 九、遗留与下一步

| 项 | 说明 | 归属 |
|----|------|------|
| Phase 1 | AI 小助手（后端 `modules/assistant` + 流式 SSE + 前端气泡/抽屉） | 后端 + 前端 |
| Phase 2 | 头像上传 + 三态审核 + 用户资料设置页 | 后端 + 前端 |
| Phase 3 | 管理后台（通用组件、`/admin/*` 路由、用户管理、API 配置热更新、四项监控） | 前端为主 |
| `CryptoUtil` | apiKey 的 AES 加密**刻意延后到 Phase 3**——它在 Phase 0 无调用方，先写即死代码 | 后端 |
| nginx | `/api/` 需加 `proxy_buffering off` 否则生产 SSE 无逐字效果（Phase 4 处理，但会影响 Phase 1 的生产验证） | 部署 |
| 代码风格 | `index.css` 代码高亮仍是暖色（`#a9583e`/`#b4654a`），与靛蓝品牌不一致，待设计确认 | 前端 |
| 提交 | 本批次尚未提交；`全局AI助手方案-v1.md`、`版本迭代记录.md` 仍未入库 | 版本管理 |

### 附：相关文件清单（本批次，共 36 改 + 10 增）

```
后端 · 迁移
project/ai-platform/pom.xml                                   （+ flyway-core / flyway-mysql）
project/ai-platform/src/main/resources/application.yml        （flyway / multipart / async-timeout / 移除 prometheus）
project/ai-platform/src/main/resources/db/migration/V1..V4.sql （新增）
project/ai-platform/db/init.sql                               （头部注明后续变更走 migration）

后端 · 权限
project/ai-platform/src/main/java/com/aiplatform/common/util/JwtUtil.java
project/ai-platform/src/main/java/com/aiplatform/common/util/SecurityUtil.java
project/ai-platform/src/main/java/com/aiplatform/common/filter/TraceIdFilter.java
project/ai-platform/src/main/java/com/aiplatform/config/JwtAuthenticationFilter.java
project/ai-platform/src/main/java/com/aiplatform/config/SecurityConfig.java
project/ai-platform/src/main/java/com/aiplatform/modules/auth/{entity/User,service/AuthService,vo/UserVO}.java

后端 · AI 网关
project/ai-platform/src/main/java/com/aiplatform/ai/{AiGateway,AiHttpClient,AiScope,AiUsageListener}.java
project/ai-platform/src/main/java/com/aiplatform/config/{AiProperties,AiRuntimeConfig,AiConfigHolder}.java
project/ai-platform/src/main/java/com/aiplatform/common/GlobalExceptionHandler.java

后端 · 会话隔离
project/ai-platform/src/main/java/com/aiplatform/modules/chat/{entity/Conversation,service/ChatService,vo/ConversationVO}.java
project/ai-platform/src/main/java/com/aiplatform/modules/{works,compare,lab}/service/*.java（调用点改传 scope）

前端
project/frontend/public/landing.html                          （从项目根移入）
project/frontend/src/{App.tsx,index.css,types/index.ts,store/authStore.ts}
project/frontend/src/components/common/ParticleField.tsx
project/frontend/src/pages/auth/LoginPage.tsx
```

---

## 迭代批次 · 2026-09-07（页面分址：宣传页 → 登录 → 工作台）

> 提交：`233cf02`（develop，已推送 `origin/develop`；上游 `2dd8fec`）
> 类型：`feat(frontend)` 路由结构调整——各页独立地址，根地址默认先进宣传页

### 一、变更总览

| 提交 | 类型 | 内容 | 影响范围 |
|------|------|------|----------|
| `233cf02` | 路由重构 | 页面分址：宣传页 `/landing`、登录 `/login`、工作台 `/app`；根地址 `/` 一律重定向宣传页 | 前端工程 |

### 二、逐项详情

- **路由分址**（`App.tsx`）：各页独立地址——
  - `/landing` 宣传页（公开）
  - `/login` 登录/注册（公开）
  - `/app` 学习工作台（受保护，未登录自动跳 `/login`）
- **根地址 `/` 一律重定向 `/landing`**：不因本地残留登录态而直进 `/app`，强制走「宣传页 → 登录 → 工作台」漏斗。
- **宣传页 CTA 一律先进登录**：`landing.html` 内「开始学习 / 免费开始」等入口统一跳 `/login`，登录成功后再进 `/app`。
- **新增 `LandingPage`**（`/landing` 承载层）：以同源 iframe 原样承载 `landing.html`，宣传页视觉 / 粒子 / 滚动渐入等动效**零改动保真**；点击页内入口在父级 SPA 路由跳转。
- **登录跳转**：`LoginPage` 登录 / 游客登录成功后目标由 `/` 改为 `/app`。

### 三、验证情况

- `tsc --noEmit` 通过；dev 下 `/`、`/landing`、`/app`、`/landing.html` 均可访问，模块编译无报错。
- 待人工确认：宣传页动效与跳转闭环的最终视觉效果。

### 四、遗留与下一步

| 项 | 说明 | 归属 |
|----|------|------|
| `/login` 登录态行为 | 保留现状：即使已有登录态访问 `/login` 也展示登录页 | 前端 |
| 产物构建 | `npm run build` 需保留 vite.config 多页入口（main + landing），否则产物缺 `/landing.html` | 构建 |
| 根目录文档入库 | 迭代记录 / Agent 方案 md 是否以 `docs:` 提交入库待定 | 文档 |

### 附：相关文件清单

```
project/frontend/src/App.tsx                       （路由分址 / 根地址重定向）
project/frontend/src/pages/landing/LandingPage.tsx （新增，/landing 承载层）
project/frontend/src/pages/auth/LoginPage.tsx      （登录成功跳 /app）
```

---

## 迭代批次 · 2026-09-06（苏格拉底实时评分面板 + 模块调整）

> 基线：`develop`（`0f4d123`，工作台全面视觉改版 / 后端云端化），Tag `v2.0.0-20260906`
> 本次批次：4 个功能提交，均叠加于基线之上，顶点 `2dd8fec`（已推送 `origin/develop`）
> 作者：范烨

### 一、变更总览

| # | 提交 | 类型 | 内容 | 影响范围 |
|---|------|------|------|----------|
| 1 | `8f3298a` | 需求迭代 | 去除「提示词实验室」模块 | 前端工程 |
| 2 | `351f21d` | 改版原型 | 作品工厂 → 灵感广场（画廊流，第一版看效果） | 前端工程 |
| 3 | `955978b` | 新功能 | 苏格拉底对话「历史记录回看」入口 | 前端工程（后端接口已具备） |
| 4 | `2dd8fec` | 新功能 | 苏格拉底对话右侧「实时学习评分面板」（启发式五要素） | 前端工程 |

四个提交在同一分支链上线性叠加，工作分支 `iter/chat-live-score` 已包含全部改动。

> 本次**后端零改动**：`2` 与 `4` 为前端原型/先行实现，`3` 的后端接口 `GET /api/chat/history`、`GET /api/chat/{id}` 早已具备，仅做前端接入。

### 二、逐项详情

#### 1. 去除「提示词实验室」模块（`8f3298a`）

需求迭代——该模块整体下线，前端入口与代码全部移除：

- **导航/入口**：侧栏「创作」分组、顶栏模块映射、工作台单页切换中删除 lab 入口。
- **类型**：`SectionId` 去掉 `lab`；`types/index.ts` 移除 `Lab*` 与 `TokenUsage` 定义。
- **删除文件**：`pages/lab/LabPage.tsx`、`services/labService.ts`。
- **清理**：`mockData` 移除 `mockLabModels`；`uiStore` / `WorkspacePage` 同步去引用。
- 验证：`tsc typecheck` 通过，Vite HMR 无报错。
- ⚠️ 遗留：后端 `/api/lab` 模块**暂保留未动**（本次只动前端）。

#### 2. 作品工厂 → 灵感广场 原型（`351f21d`）

作品工厂改版为「灵感广场」画廊形态，第一版用于看效果：

- 画廊卡片流 + **搜索 / 分类 / 最热 / 最新**排序。
- 详情弹窗；点赞 / 使用计数。
- 右下角 `+` FAB 上传投稿弹窗。
- **数据来源**：前端种子 + 内存数据，`galleryData` 作为**数据契约**。
- 新增组件：`PromptCard`、`PromptModals`、`promptMeta`，重写 `WorksPage`。
- ⏭️ 后续：换后端社区接口即可接真数据。

#### 3. 苏格拉底对话 · 历史记录回看（`955978b`）

- `chatStore.loadHistory(id)`：按 id 拉取完整对话（气泡流 + 结果）载入。
- 新增 `HistoryDrawer`：右侧抽屉列出**进行中 / 已完成**对话（标题 / 轮数 / 迷你星级 / 时间）；游客模式提示登录后保存。
- `ChatPage` 双入口：欢迎屏「查看历史对话记录」链接 + 对话中头部「历史记录」按钮。
- 回看整段对话时**评分状态正确回显**（已评分的不再重复邀请打分）。
- 后端接口已具备，本次仅前端接入。

#### 4. 苏格拉底对话 · 实时学习评分面板（`2dd8fec`）

本次迭代的核心增强——把「学习评分」从对话结束的回顾，变为**对话过程中的实时面板**：

- 新增 `src/lib/promptScoring.ts` 评分引擎：
  - 启发式**五要素**：角色 / 任务 / 上下文 / 格式 / 约束，各 20 分（总分 100）。
  - 原始提示词命中为**基线**；AI 追问定位待补要素；用户作答补全。
  - **覆盖度 + 具体度**折算；补全前提下提前收尾额外加分。
- 新增 `LiveProgressPanel` 侧栏：
  - 大分卡实时预估 → 完成后给出本轮学习分。
  - 五要素清单状态：已具备 / 部分 / 引导中 / 待补全。
  - 「下一步」引导卡片（下一步该补哪个要素）。
- `ChatPage` 集成：header 评分开关 + 消息区与侧栏 flex 布局。
- 🔒 **口径锁定**：雷达 = 五要素；本轮只做实时面板，**学习地图 / 雷达同步留待后续接入**。

### 三、验证情况

- 四提交均前端改动；`#1` 明示 `tsc typecheck` 通过、Vite HMR 无报错。
- `#4` 改动全部为新增/小改，未触碰既有对话主链路逻辑。

### 四、遗留与下一步

| 项 | 说明 | 归属 |
|----|------|------|
| 学习地图 / 能力雷达同步 | 评分口径已锁定为五要素，待把实时分写回进度与雷达 | 前端 + 后端 |
| 灵感广场接后端 | `galleryData` 为契约，待换社区接口 | 前端（后端需提供社区接口） |
| 后端 `/api/lab` 清理 | 前端已下线，后端模块待同步移除或保留决策 | 后端 |
| 历史回看的会话恢复 | 当前是整段回看，后续可做「从历史续聊」 | 产品决策 |

### 附：相关文件清单

```
project/frontend/src/lib/promptScoring.ts        （新增，评分引擎 220 行）
project/frontend/src/components/chat/LiveProgressPanel.tsx（新增，侧栏面板）
project/frontend/src/components/chat/HistoryDrawer.tsx    （新增，历史抽屉）
project/frontend/src/pages/works/WorksPage.tsx   （灵感广场重写）
project/frontend/src/pages/works/components/PromptCard.tsx / PromptModals.tsx / promptMeta.tsx（新增）
project/frontend/src/pages/works/galleryData.ts  （新增，社区数据契约）
project/frontend/src/pages/chat/ChatPage.tsx     （双入口 + 评分面板集成）
project/frontend/src/store/chatStore.ts          （loadHistory）
project/frontend/src/pages/lab/LabPage.tsx、src/services/labService.ts（已删除）
```
