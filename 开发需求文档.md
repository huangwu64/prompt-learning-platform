# 零基础学AI平台 - 开发需求文档

| 项目名称 | 零基础学AI平台 |
|---------|--------------|
| 文档版本 | v2.0 |
| 更新日期 | 2026-08-11 |
| 文档状态 | 开发中 |
| 技术栈 | 前端 React 18.3 + TypeScript 5.8 + Vite 6.3 / 后端 Java 21 + Spring Boot 3 + MyBatis-Plus + MySQL 8.0 + Redis 7 |
| AI 服务 | DeepSeek API (deepseek-chat) |

---

## 一、项目背景

### 1.1 业务触发源

ChatGPT、DeepSeek 等大语言模型在 2025—2026 年间快速普及，"会用 AI"正在从技术圈的新鲜事变成普通职场人和学生的基础技能。然而，项目发起人在与身边非技术用户的交流中发现一个普遍现象：**同样一个需求，会写提示词的人能拿到 90 分的结果，不会写的人只能得到 50 分**。差距不在于工具，而在于"怎么问"。

市场上现有的 AI 教育产品以视频课程和图文教程为主，属于被动接收型学习。用户看完教程后仍然不知道自己写的提示词好在哪里、差在哪里，缺乏即时反馈和刻意练习的环境。`[PM 判断]`

### 1.2 用户痛点

| 痛点 | 具体表现 | 当前解决方案的不足 |
|------|---------|-------------------|
| 不知道怎么问 | 用户面对 AI 输入框时写不出结构化需求，只能输入一句话 | 网上搜"提示词模板"，但模板脱离场景，套用效果差 |
| 不知道差在哪 | 用户写完提示词拿到结果，无法判断是提示词的问题还是模型的问题 | 没有对比机制，无法看到"优化前 vs 优化后"的差异 |
| 学完就忘 | 看完教程当时觉得懂了，下次用 AI 时又回到老习惯 | 缺乏持续练习和成长追踪机制 |
| 路径模糊 | 想系统学习提示词工程，但不知道从哪开始、学到什么程度算掌握 | 现有教程没有清晰的阶段划分和进度可视化 |

---

## 二、项目目标

### 2.1 定性目标

通过"苏格拉底式追问"的对话学习模式，让零基础用户在 7 天内建立提示词工程的基本认知，能够独立写出包含角色设定、任务描述、上下文、输出格式、约束条件五要素的结构化提示词。

### 2.2 量化指标

| 指标 | 当前基线 | 目标值 | 依据 |
|------|---------|--------|------|
| 核心对话链路可用性 | 未实现 | — | 待开发 |
| 首批种子用户 | 0 | 100 人 | 3 个月内 |
| 用户次日留存率 | 未上线 | ≥ 40% | 6 个月内 |
| Mock 数据清除率 | 全部模块使用 Mock | 0% | 上线前全量核查 |
| 模块后端接通率 | 0/8 模块接通 | 8/8 | M8—M12 完成后 |

---

## 三、用户画像与使用场景

### 3.1 目标用户画像

| 用户类型 | 占比预估 | 核心诉求 | 典型行为特征 |
|---------|---------|---------|-------------|
| 职场新人 | 35% | 用 AI 提效写报告/邮件/文案 | 高频使用，追求即用即走 |
| 学生群体 | 25% | 学习用 AI 辅助学习与写作 | 有学习意愿，时间充裕 |
| 内容创作者 | 20% | 批量生产高质量内容 | 关注输出质量和效率 |
| 自由职业者 | 10% | 提升 AI 接单交付质量 | 关注专业度和差异化 |
| 好奇探索者 | 10% | 系统了解提示词技巧 | 探索型，停留时间长 |

### 3.2 核心使用场景

**场景一：引导学习（主流程）**

用户输入一句模糊需求（如"帮我写个邮件"），AI 通过 2—5 轮追问（收件人是谁？希望传达什么情绪？）引导用户思考提示词的不足。对话结束后，用户看到"原始提示词 vs 优化提示词"的对比和差异分析，理解"差在哪里"。打分后，进度同步到学习地图。

**场景二：即用模板（快捷流程）**

用户临时需要写一封商务邮件，进入作品工厂选择邮件模板，填写收件人和主题，一键生成可用文案，复制即走。

**场景三：可视化拼装（探索流程）**

用户不懂提示词结构，在积木构建器中点选"角色设定""任务描述""约束条件"等积木块，右侧实时预览拼装结果，点击"测试"查看 AI 回复效果。

**场景四：每日精进（留存流程）**

用户参加每日挑战赛，根据给定任务写提示词，AI 从四个维度评分并给出反馈。查看排行榜了解自己的位置，连续打卡获得徽章。

---

## 四、功能需求详述

### 4.1 模块一：学习地图（首页）

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  Header（Logo + 导航 + 用户头像）                 │
├─────────────────────────────────────────────────┤
│  统计卡片区                                      │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐         │
│  │ 平均评分  │ │ 连续天数  │ │ 已掌握    │         │
│  │  4.2/5   │ │   7 天   │ │  12 个    │         │
│  └──────────┘ └──────────┘ └──────────┘         │
├─────────────────────────────────────────────────┤
│  四阶段技能树（竖向路径图）                        │
│                                                   │
│   [🌱 入门] ──→ [🚀 进阶] ──→ [💎 精通] ──→ [👑 大师] │
│    已完成        进行中        未解锁        未解锁    │
│                                                   │
│   当前阶段展开知识点树：                           │
│   ├── 角色设定 ✅                                 │
│   ├── 任务描述 ✅                                 │
│   ├── 上下文   🔵 (学习中)                        │
│   ├── 输出格式  🔒                                │
│   └── 约束条件  🔒                                │
├─────────────────────────────────────────────────┤
│  成就徽章墙（横向滚动）                            │
└─────────────────────────────────────────────────┘
```

#### 业务逻辑

学习地图是平台首页，承担流量入口和学习进度可视化双重职责。页面顶部展示三个核心统计卡片：**平均评分**（基于 `conversations` 表 `rating` 字段计算最近 20 条对话的平均分，满分 5 分）、**连续打卡天数**（`users.streakDays`）、**已掌握知识点数**。

四阶段技能树将"学 AI"拆解为可量化的成长路径。每个阶段包含若干知识点，用户需完成该阶段所有知识点练习且平均评分达到阈值后，自动解锁下一阶段。阶段定义如下：

| 阶段 | 名称 | 核心内容 | 解锁条件 |
|------|------|---------|---------|
| 1 | 入门 Beginner | 提示词基本结构：角色、任务、上下文、格式、约束 | 注册即解锁 |
| 2 | 进阶 Intermediate | 高级技巧：思维链、多轮对话、自我反思 | 入门阶段全部知识点掌握，平均评分 ≥ 3.5 |
| 3 | 精通 Advanced | 特定领域提示词：代码、写作、分析 | 进阶阶段全部知识点掌握，平均评分 ≥ 4.0 |
| 4 | 大师 Master | 构建复杂工作流，优化模型性能 | 精通阶段全部知识点掌握，平均评分 ≥ 4.5 |

成就徽章墙展示用户已获得的徽章，包括"连续打卡 7 天""挑战赛 100 分""完成入门阶段"等里程碑。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 页面加载 | 调用 `GET /api/learning/progress` 获取进度数据 | 渲染统计卡片、技能树、徽章墙 |
| 点击阶段节点 | 展开该阶段知识点树 | 节点展开动画，显示知识点列表 |
| 点击知识点 | 跳转到苏格拉底对话模块 | URL 变为 `/chat?topic=xxx` |
| 达成里程碑 | 触发庆祝动画 | 弹出徽章解锁提示，播放动效 |

#### 字段规则

| 字段名称 | 类型 | 来源 | 说明 |
|---------|------|------|------|
| averageRating | Float | conversations.rating 聚合 | 最近 20 条对话平均分，保留一位小数 |
| streakDays | Integer | users.streakDays | 连续打卡天数 |
| masteredCount | Integer | 计算字段 | 状态为"已掌握"的知识点数量 |
| currentStage | Enum | 计算字段 | beginner / intermediate / advanced / master |
| knowledgePoints | Array | 配置表 | 每个知识点含 id、name、status、stage |

#### 状态转换（知识点状态）

| 当前状态 | 允许操作 | 下一状态 | 触发条件 |
|---------|---------|---------|---------|
| 未学习（灰色锁） | — | 学习中 | 前置知识点已掌握 |
| 学习中（蓝色高亮） | 开始练习 | 已掌握 | 该知识点相关对话评分 ≥ 3.5 |
| 已掌握（绿色勾） | 再次练习 | 已掌握 | 用户主动重练 |

#### 边界与异常

- 用户无对话记录时，平均评分显示"—"，技能树全部锁定在入门阶段
- 网络请求失败时，展示上次缓存数据并显示"数据可能不是最新"提示
- 阶段解锁条件校验在后端执行，前端仅做展示

---

### 4.2 模块二：苏格拉底对话（核心模块）

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  对话区                          │ 进度条         │
│                                  │ 第 2/5 轮     │
│  ┌──────────────────────────┐   │               │
│  │ AI: 你的提示词缺少角色设定 │   │  [原始提示词]  │
│  │     你希望 AI 扮演什么角色？│   │  "帮我写个邮件" │
│  └──────────────────────────┘   │               │
│                                  │  [优化提示词]  │
│  ┌──────────────────────────┐   │  （对话完成后  │
│  │ 用户: 一位资深HR           │   │   显示）       │
│  └──────────────────────────┘   │               │
│                                  │  [改进分析]    │
│  ┌──────────────────────────┐   │  （对话完成后  │
│  │ AI: 收件人是谁？邮件的     │   │   显示）       │
│  │     目的是什么？          │   │               │
│  └──────────────────────────┘   │               │
│                                  │               │
│  ┌────────────────────────────┐ │               │
│  │ 输入框               [发送] │ │  [一键完成]    │
│  └────────────────────────────┘ │  [保存到作品库] │
└─────────────────────────────────────────────────┘
```

#### 业务逻辑

苏格拉底对话是平台的核心教学模块。用户输入一句模糊需求后，AI 扮演提示词工程老师的角色，通过 2—5 轮苏格拉底式追问（引导、反问、启发）帮助用户自行完善提示词。AI 不直接给答案，而是通过提问让用户意识到自己提示词的不足。

**追问逻辑**：AI 评估当前提示词的完整性，针对缺失的五要素（角色、任务、上下文、格式、约束）逐项追问。每轮追问聚焦一个维度，避免信息过载。当 AI 判断已收集到足够信息时，主动停止追问并生成优化提示词。

**停止判断**：AI 内部维护一个"信息完整度评分"（0—100），每轮对话后更新。当评分 ≥ 80 或已达到 5 轮上限时，进入结果展示阶段。用户也可随时点击"一键完成"强制结束对话。

**结果对比**：对话结束后，界面左侧展示用户原始提示词，右侧展示 AI 优化后的提示词，下方展示 AI 的改进分析（如"增加了角色设定""补充了目标受众""结构化了输出格式"）。

**评分关联**：用户对本次学习体验打分（1—5 分），该分数写入 `conversations.rating`，同步影响学习地图的平均评分和知识点掌握状态。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 输入初始提示词并发送 | 调用 `POST /api/chat` 创建对话 | 生成对话 ID，AI 返回第一轮追问 |
| 回复 AI 追问 | 调用 `POST /api/chat/:id/message` | AI 返回下一轮追问或完成判断 |
| 点击"一键完成" | 调用 `POST /api/chat/:id/complete` | 强制生成优化结果，跳到对比页 |
| 点击"保存到作品库" | 调用作品库保存接口 | 弹出保存成功提示 |
| 打分（1—5 星） | 调用 `POST /api/chat/:id/rating` | 更新评分，提示"进度已同步" |

#### 字段规则

| 字段名称 | 类型 | 必填 | 长度/范围 | 默认值 |
|---------|------|------|---------|--------|
| originalPrompt | String | 是 | 1—500 字符 | 无 |
| message.content | String | 是 | 1—1000 字符 | 无 |
| rating | Integer | 否 | 1—5 | 无 |
| conversation.status | Enum | — | active / completed | active |

#### 状态转换（对话状态）

| 当前状态 | 允许操作 | 下一状态 | 触发条件 |
|---------|---------|---------|---------|
| active（进行中） | 发送消息、一键完成 | active / completed | AI 判断信息充足或用户点击完成 |
| completed（已完成） | 评分、保存到作品库 | completed | 终态，不可回退 |

#### 边界与异常

- AI 追问超过 5 轮时，强制进入结果生成阶段
- 用户输入为空或纯空格时，前端拦截，不发请求
- AI 服务超时（> 30s）时，显示"AI 正在思考中，请稍候"并提供重试按钮
- 对话历史加载失败时，显示"加载历史记录失败"并提供刷新按钮
- 并发创建对话限制：同一用户同时只能有一个 active 状态的对话

---

### 4.3 模块三：作品工厂

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  模板选择区（横向卡片）                           │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐            │
│  │📄 PPT │ │📝 报告│ │📧 邮件│ │🌐 社交│            │
│  │ 大纲  │ │ 文案  │ │      │ │ 媒体  │            │
│  └──────┘ └──────┘ └──────┘ └──────┘            │
├─────────────────────────────────────────────────┤
│  表单区（根据选中模板动态渲染）                    │
│  ┌─────────────────────────────────────────────┐ │
│  │ 收件人:    [________________]                │ │
│  │ 邮件目的:  [________________]                │ │
│  │ 语气风格:  [正式 ▼]                          │ │
│  │ 附加说明:  [________________]                │ │
│  │                            [生成]            │ │
│  └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│  结果区（生成后显示）                              │
│  ┌─────────────────────────────────────────────┐ │
│  │ 尊敬的张总：                                  │ │
│  │                                              │ │
│  │ 关于本季度项目进展...                          │ │
│  │                                              │ │
│  │              [复制] [保存] [重新生成]          │ │
│  └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│  历史作品（折叠列表）                              │
└─────────────────────────────────────────────────┘
```

#### 业务逻辑

作品工厂是快速生成工具箱，面向"即用即走"场景。用户选择场景模板后，填写关键信息表单，一键调用 DeepSeek 生成内容。生成的作品自动保存到个人作品库，支持再次编辑和复制。

**模板体系**：当前覆盖四类高频场景，每类模板对应一套表单字段和提示词模板：

| 模板类型 | 表单字段 | 提示词策略 |
|---------|---------|-----------|
| PPT 大纲 | 主题、受众、时长、核心要点 | 结构化为章节—页面—要点三级层次 |
| 报告/文案 | 标题、要点列表、风格、字数 | 围绕要点展开论述，控制篇幅 |
| 商务邮件 | 收件人、目的、语气、附件说明 | 根据收件人身份调整称呼和措辞 |
| 社交媒体 | 平台、产品、风格、目标 | 匹配平台调性（小红书种草风/抖音脚本风） |

**历史版本管理**：每次生成自动保存一条记录到 `works` 表，用户可在历史列表中查看、再次编辑或删除。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 点击模板卡片 | 高亮选中模板 | 下方渲染对应表单 |
| 填写表单并点击"生成" | 调用 `POST /api/works` | 按钮变为 loading 状态，结果区显示"生成中" |
| 生成完成 | 展示结果内容 | 结果区渲染 Markdown 格式内容 |
| 点击"复制" | 复制到剪贴板 | 显示"已复制"提示 |
| 点击"保存" | 调用保存接口 | 显示"已保存到作品库" |
| 点击历史作品 | 加载到结果区 | 可编辑后重新生成 |

#### 字段规则

| 字段名称 | 类型 | 必填 | 长度/范围 | 默认值 |
|---------|------|------|---------|--------|
| workType | Enum | 是 | ppt / report / email / social | 无 |
| title | String | 是 | 1—100 字符 | 无 |
| formData | JSON | 是 | 根据模板类型动态校验 | 无 |
| content | Text | 否 | — | 生成后填充 |
| shareUrl | String | 否 | — | 无 |

#### 状态转换（作品状态）

| 当前状态 | 允许操作 | 下一状态 | 触发条件 |
|---------|---------|---------|---------|
| generating（生成中） | 取消 | generated / failed | AI 返回结果 / 超时或报错 |
| generated（已生成） | 编辑、复制、保存、删除 | generated | 终态，可重复编辑 |

#### 边界与异常

- 表单必填字段未填时，"生成"按钮置灰
- AI 生成超时（> 30s）时，显示"生成超时，请重试"
- 历史作品列表为空时，显示空状态引导"还没有作品，选择上方模板开始创建"
- 单次生成内容超过模型 token 限制时，截断并提示"内容较长，已截断显示"

---

### 4.4 模块四：提示词挑战赛

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  今日挑战                                        │
│  ┌─────────────────────────────────────────────┐ │
│  │ 任务: 让 AI 扮演一位严苛的代码审查员          │ │
│  │ 难度: ⭐⭐⭐  |  参与人数: 156               │ │
│  │ 截止: 2026-08-11 23:59                      │ │
│  └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│  提示词输入区                                    │
│  ┌─────────────────────────────────────────────┐ │
│  │                                              │ │
│  │  (输入你的提示词...)                          │ │
│  │                                              │ │
│  │                              [提交评分]       │ │
│  └─────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────┤
│  评分结果（提交后显示）                           │
│  ┌──────────┬──────────┬──────────┬──────────┐  │
│  │ 有效性    │ 结构化   │ 创造性   │ 约束性   │  │
│  │   85     │   72     │   90     │   65     │  │
│  └──────────┴──────────┴──────────┴──────────┘  │
│  总分: 78/100                                    │
│  AI 反馈: 角色设定很好，但缺少输出格式要求...      │
├─────────────────────────────────────────────────┤
│  排行榜 [今日] [本周] [总榜]                      │
│  #1  用户A   95 分                               │
│  #2  用户B   92 分                               │
│  #3  你      78 分                               │
└─────────────────────────────────────────────────┘
```

#### 业务逻辑

挑战赛通过游戏化竞技机制激发学习热情。系统每天发布一个固定任务，用户根据任务描述编写提示词，提交后 AI 从四个维度评分（满分 100），并给出详细反馈。评分结果进入排行榜，支持按今日、本周、总榜切换查看。

**评分维度**：

| 维度 | 权重 | 评分标准 |
|------|------|---------|
| 有效性 | 30% | 提示词能否引导 AI 完成指定任务 |
| 结构化 | 25% | 是否包含角色、任务、上下文、格式、约束等结构要素 |
| 创造性 | 25% | 提示词的表达方式和策略是否有创意 |
| 约束性 | 20% | 是否对输出格式、字数、语气等做出明确约束 |

**排行榜规则**：同一用户同一天多次提交，取最高分计入排行榜。排行榜按总分降序排列，显示用户名、分数和排名。达到特定分数解锁特殊徽章和称号（如 90 分以上解锁"提示词达人"称号）。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 页面加载 | 调用 `GET /api/challenges` 获取今日挑战 | 渲染任务卡片 |
| 输入提示词并提交 | 调用 `POST /api/challenges/:id/submit` | 按钮变为 loading，显示"AI 评分中" |
| 评分完成 | 展示四维评分 + 总分 + AI 反馈 | 评分区域展开，排行榜更新 |
| 切换排行榜 Tab | 调用 `GET /api/challenges/leaderboard?type=xxx` | 排行榜列表刷新 |

#### 字段规则

| 字段名称 | 类型 | 必填 | 长度/范围 | 默认值 |
|---------|------|------|---------|--------|
| challengeId | String | 是 | UUID | 无 |
| prompt | String | 是 | 10—2000 字符 | 无 |
| score | Integer | — | 0—100 | AI 生成 |
| feedback | Text | — | — | AI 生成 |
| rank | Integer | — | — | 计算字段 |

#### 边界与异常

- 提示词少于 10 字符时，提示"提示词过短，请补充更多细节"
- 每日挑战截止后，不可再提交，显示"今日挑战已结束"
- AI 评分失败时，提供"重新评分"按钮，不扣除提交次数
- 排行榜数据延迟 5 分钟更新，提示"排行榜数据可能有延迟"

---

### 4.5 模块五：提示词实验室

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  提示词输入区（左侧）              │ 控制面板      │
│  ┌─────────────────────────────┐  │              │
│  │                              │  │ 模型选择:    │
│  │  输入要测试的提示词...        │  │ ☑ DeepSeek  │
│  │                              │  │ ☑ GPT-4o    │
│  │                              │  │ ☐ Claude    │
│  │                              │  │              │
│  │                              │  │ 温度: 0.7    │
│  │                              │  │ [─────●──]  │
│  │              [运行实验]       │  │              │
│  └─────────────────────────────┘  │ Max Token:   │
│                                    │ 2048         │
│  结果对比区（下方，并排显示）        │ [────●────]  │
│  ┌────────────┬────────────┐      │              │
│  │ DeepSeek   │ GPT-4o     │      │ [历史记录]   │
│  │ 回复内容... │ 回复内容... │      │              │
│  │            │            │      │              │
│  └────────────┴────────────┘      │              │
└─────────────────────────────────────────────────┘
```

#### 业务逻辑

实验室提供沙盒环境，让用户对比不同 AI 模型、不同参数下的输出效果，理解提示词调优原理。用户输入提示词，勾选要对比的模型，调节温度和最大 token 参数，点击"运行实验"后所有选中模型同时生成回复，并排展示。

**多模型对比**：支持同时调用多个大语言模型。当前阶段以 DeepSeek 为主，计划接入 GPT-4o 和 Claude。当某个模型 API 不可用时，降级为单模型多参数对比（不同温度值下的输出对比）。

**参数说明**：

| 参数 | 作用 | 范围 | 默认值 |
|------|------|------|--------|
| Temperature | 控制创造性，值越高回复越发散，越低越稳定 | 0.0—2.0 | 0.7 |
| Max Tokens | 控制输出长度 | 1—8192 | 2048 |

**历史对比记录**：每次实验自动保存到本地和后端，用户可回溯查看不同参数组合下的输出差异。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 输入提示词 | — | 启用"运行实验"按钮 |
| 勾选/取消模型 | 更新选中模型列表 | 至少选中一个模型才能运行 |
| 调节参数滑块 | 实时显示当前值 | 参数值跟随滑块变化 |
| 点击"运行实验" | 调用 `POST /api/lab/compare` | 各模型区域显示 loading |
| 结果返回 | 并排展示各模型回复 | 每个模型区域渲染 Markdown |
| 点击"历史记录" | 调用历史接口 | 弹出历史实验列表 |

#### 字段规则

| 字段名称 | 类型 | 必填 | 长度/范围 | 默认值 |
|---------|------|------|---------|--------|
| prompt | String | 是 | 1—4000 字符 | 无 |
| models | Array<String> | 是 | 1—3 个 | ["deepseek"] |
| temperature | Float | 否 | 0.0—2.0 | 0.7 |
| maxTokens | Integer | 否 | 1—8192 | 2048 |

#### 状态转换（实验状态）

| 当前状态 | 允许操作 | 下一状态 | 触发条件 |
|---------|---------|---------|---------|
| idle（空闲） | 编辑参数、运行 | running | 点击"运行实验" |
| running（运行中） | 取消 | completed / partial_failed | 全部完成 / 部分模型失败 |
| completed（已完成） | 查看结果、再次运行 | completed | 终态 |

#### 边界与异常

- 未选中任何模型时，"运行实验"按钮置灰
- 某个模型 API 超时或报错时，该区域显示"此模型暂时不可用"，其他模型正常展示
- 全部模型失败时，显示"实验失败，请稍后重试"
- 并发实验限制：同一用户每分钟最多运行 3 次实验

---

### 4.6 模块六：积木构建器

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  积木库（左侧）    │ 拼装区（中间）  │ 预览区（右侧）│
│  ┌──────────────┐ │ ┌────────────┐ │ ┌──────────┐ │
│  │🟣 角色设定    │ │ │🟣 你是一位  │ │ │完整提示词 │ │
│  │  AI助手       │ │ │  资深编辑   │ │ │          │ │
│  │  领域专家     │ │ │            │ │ │你是一位   │ │
│  │  耐心教师     │ │ │🌹 帮我撰写  │ │ │资深编辑   │ │
│  ├──────────────┤ │ │  一篇关于   │ │ │          │ │
│  │🌹 任务描述    │ │ │  AI的科普   │ │ │帮我撰写   │ │
│  │  撰写文章     │ │ │  文章       │ │ │一篇关于   │ │
│  │  分析数据     │ │ │            │ │ │AI的科普   │ │
│  │  编写代码     │ │ │🔵 目标读者  │ │ │文章      │ │
│  ├──────────────┤ │ │  是初学者   │ │ │          │ │
│  │🔵 上下文      │ │ │            │ │ │目标读者  │ │
│  │  背景信息     │ │ │🟢 Markdown │ │ │是初学者  │ │
│  │  目标读者     │ │ │  表格格式   │ │ │          │ │
│  ├──────────────┤ │ │            │ │ │Markdown  │ │
│  │🟢 输出格式    │ │ │            │ │ │表格格式  │ │
│  │  要点列表     │ │ │            │ │ │          │ │
│  │  JSON         │ │ │            │ │ │[测试]    │ │
│  │  Markdown表格 │ │ │            │ │ │[保存模板]│ │
│  ├──────────────┤ │ │            │ │ │[复制]    │ │
│  │🟡 约束条件    │ │ │            │ │ │          │ │
│  │  字数限制     │ │ │            │ │ │测试结果:  │ │
│  │  语气风格     │ │ │            │ │ │(点击测试  │ │
│  │  禁止项       │ │ │            │ │ │ 后显示)   │ │
│  └──────────────┘ │ └────────────┘ │ └──────────┘ │
└─────────────────────────────────────────────────┘
```

#### 业务逻辑

积木构建器是可视化提示词拼装工具，帮助不理解提示词结构的用户通过点选积木块快速组装专业提示词。五大类积木对应提示词五要素：

| 积木类别 | 颜色 | 对应要素 | 预设选项 |
|---------|------|---------|---------|
| 角色设定 | 紫色 | Role | AI 助手、领域专家、耐心教师、资深编辑、代码审查员 |
| 任务描述 | 红色 | Task | 撰写文章、分析数据、编写代码、翻译文本、总结摘要 |
| 上下文 | 蓝色 | Context | 背景信息、目标读者、使用场景、前置条件 |
| 输出格式 | 绿色 | Format | 要点列表、JSON、Markdown 表格、分步骤说明、带标题段落 |
| 约束条件 | 黄色 | Constraint | 字数限制、语气风格、禁止项、必须包含的关键词 |

用户从左侧积木库点击需要的积木块，积木添加到中间拼装区。在拼装区可拖拽调整积木顺序、编辑积木内容。右侧实时预览拼装后的完整提示词文本。点击"测试"按钮调用 DeepSeek API 查看这段提示词的实战效果。拼装好的提示词可保存为个人模板或直接复制使用。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 点击积木库中的积木 | 添加到拼装区 | 拼装区新增积木块，预览区更新 |
| 编辑积木内容 | 更新积木文本 | 预览区实时更新 |
| 拖拽调整积木顺序 | 重排积木位置 | 预览区按新顺序更新 |
| 删除积木 | 从拼装区移除 | 预览区更新 |
| 点击"测试" | 调用 `POST /api/lab/test` | 预览区下方显示 AI 回复 |
| 点击"保存模板" | 调用模板保存接口 | 弹出"保存成功"提示 |
| 点击"复制" | 复制完整提示词到剪贴板 | 显示"已复制"提示 |

#### 字段规则

| 字段名称 | 类型 | 必填 | 长度/范围 | 默认值 |
|---------|------|------|---------|--------|
| blocks | Array<Block> | 是 | 1—20 个 | 无 |
| block.type | Enum | 是 | role / task / context / format / constraint | 无 |
| block.content | String | 是 | 1—500 字符 | 无 |
| block.order | Integer | 是 | 0—19 | 按添加顺序 |
| templateName | String | 保存时必填 | 1—50 字符 | 无 |

#### 边界与异常

- 拼装区为空时，预览区显示"点击左侧积木开始拼装"
- 积木数量超过 20 个时，提示"积木数量已达上限"
- 测试请求超时时，显示"AI 响应超时，请重试"
- 保存模板时未命名，提示"请输入模板名称"
- 同名模板已存在时，提示"模板名称已存在，请更换"

---

### 4.7 模块七：AI 能力雷达 / 个人中心

#### 页面布局

```
┌─────────────────────────────────────────────────┐
│  用户信息                                        │
│  [头像] 用户名  |  注册天数: 30 天  |  等级: 进阶  │
├──────────────────────────┬──────────────────────┤
│  能力雷达图               │  学习统计             │
│                          │  总对话数: 45         │
│        精确度  85         │  平均评分: 4.2        │
│          /\              │  连续打卡: 7 天       │
│         /  \             │  已掌握: 12 知识点     │
│  迭代力/    \ 结构化      │                      │
│   72  /      \  78       │  徽章墙               │
│       \      /           │  [7天打卡] [入门完成]  │
│        \    /            │  [挑战100] [首次评分]  │
│         \  /             │                      │
│   创造力  --  约束性      │  个性化建议           │
│     80        68         │  "你的约束性评分偏低，  │
│                          │   建议多练习输出格式    │
│                          │   相关的提示词"        │
└──────────────────────────┴──────────────────────┘
```

#### 业务逻辑

个人中心展示用户的五维能力雷达图和学习统计，提供个性化改进建议。五维能力基于用户在对话和挑战赛中的表现自动统计：

| 维度 | 数据来源 | 计算方式 |
|------|---------|---------|
| 精确度 | 对话评分 + 挑战赛有效性分数 | 加权平均 |
| 结构化 | 挑战赛结构化分数 | 直接取值 |
| 创造力 | 挑战赛创造性分数 | 直接取值 |
| 约束性 | 挑战赛约束性分数 | 直接取值 |
| 迭代力 | 对话轮次和评分提升趋势 | 趋势计算 |

个性化建议基于雷达图中得分最低的维度生成，引导用户针对性练习。

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 页面加载 | 调用 `GET /api/profile/skills` + `GET /api/profile/stats` | 渲染雷达图、统计、徽章 |
| 悬停雷达图顶点 | 显示该维度详细分数 | Tooltip 展示 |
| 点击徽章 | 展示徽章获取条件 | 弹出徽章详情 |

#### 字段规则

| 字段名称 | 类型 | 来源 | 说明 |
|---------|------|------|------|
| accuracy | Float | 计算字段 | 精确度评分 0—100 |
| structure | Float | 计算字段 | 结构化评分 0—100 |
| creativity | Float | 计算字段 | 创造力评分 0—100 |
| constraint | Float | 计算字段 | 约束性评分 0—100 |
| iteration | Float | 计算字段 | 迭代力评分 0—100 |
| totalConversations | Integer | conversations 计数 | 总对话数 |
| badges | Array | user_badges 关联 | 已获得徽章列表 |

#### 边界与异常

- 新用户无数据时，雷达图全维度显示 0，建议区显示"完成第一次对话后查看你的能力雷达"
- 统计数据加载失败时，显示骨架屏并提示"数据加载中"

---

### 4.8 模块八：认证体系

#### 页面布局

```
┌─────────────────────────────────┐
│         登录 / 注册              │
│                                 │
│  ┌───────────────────────────┐  │
│  │  邮箱:  [______________]  │  │
│  │  密码:  [______________]  │  │
│  │                           │  │
│  │      [登录]  [注册]       │  │
│  │                           │  │
│  │  测试账号: test@example.com│  │
│  │  密码: 123456             │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

#### 业务逻辑

认证体系提供用户注册、登录、JWT 鉴权功能。注册时校验邮箱格式和密码强度，密码通过 Spring Security 的 BCryptPasswordEncoder 加密后存入数据库。登录成功后返回 JWT token，前端存储在 localStorage，后续请求通过 Authorization 头携带。

**鉴权流程**：

```
用户登录 → 后端验证邮箱+密码 → 生成 JWT (有效期 7 天) → 返回 token + 用户信息
→ 前端存储 token → 后续请求携带 Authorization: Bearer <token>
→ 后端中间件验证 token → 通过则放行，失败返回 401
```

#### 交互逻辑

| 用户操作 | 系统响应 | 页面/状态变化 |
|---------|---------|-------------|
| 输入邮箱密码点击"登录" | 调用 `POST /api/auth/login` | 验证成功跳转首页，失败显示错误 |
| 输入邮箱密码点击"注册" | 调用 `POST /api/auth/register` | 注册成功自动登录并跳转首页 |
| Token 过期 | API 返回 401 | 前端清除 token，跳转登录页 |

#### 字段规则

| 字段名称 | 类型 | 必填 | 长度/范围 | 校验规则 |
|---------|------|------|---------|---------|
| email | String | 是 | 5—100 字符 | 合法邮箱格式 |
| password | String | 是 | 6—50 字符 | 至少 6 位 |
| username | String | 注册时必填 | 2—30 字符 | 唯一 |

#### 边界与异常

- 邮箱已注册时，返回"该邮箱已注册"
- 邮箱格式不合法时，前端拦截不发请求
- 密码错误时，返回"邮箱或密码错误"（不透露具体是哪个错）
- Token 过期或无效时，返回 401，前端跳转登录页
- 连续登录失败 5 次后，锁定该账号 15 分钟

---

## 五、AI 产品专项

### 5.1 准确度定义

本平台的 AI 能力分为两类，各自定义不同的"准确度"标准：

| AI 能力 | 准确度定义 | 评估方式 |
|---------|-----------|---------|
| 苏格拉底追问质量 | 追问是否覆盖了用户提示词缺失的关键要素，且引导方向正确 | 用户评分（1—5 分）+ 人工抽检 |
| 挑战赛评分准确性 | AI 给出的四维评分与人工专家评分的一致性 | 每月抽样 50 条，与专家评分对比，偏差 ≤ 15 分为合格 |
| 提示词优化质量 | 优化后的提示词是否比原始提示词在标准测试任务上产出更好的结果 | A/B 测试：同一任务分别用原始和优化提示词，人工盲评 |

### 5.2 降级机制

| 故障场景 | 降级策略 | 用户感知 |
|---------|---------|---------|
| DeepSeek API 不可用 | 苏格拉底对话暂停，展示"AI 服务暂时不可用" | 明确提示，不影响其他模块 |
| 多模型对比中部分模型失败 | 仅展示可用模型的结果，失败模型区域显示降级提示 | 部分结果可见 |
| AI 评分服务超时 | 允许用户重新提交，不扣除提交次数 | 透明重试 |
| AI 生成内容为空或异常 | 返回"生成失败，请重试"，不计入作品库 | 可重试 |

### 5.3 成本控制

| 控制项 | 策略 |
|--------|------|
| 单用户调用限制 | 每日最多 50 次 AI 调用（对话 + 生成 + 测试） |
| 单次请求 token 限制 | 输入 ≤ 4000 token，输出 ≤ 2048 token |
| 全局熔断 | 日调用量超过阈值时，新用户请求排队 |

---

## 六、非功能需求

### 6.1 性能需求

| 指标 | 目标值 |
|------|--------|
| 首页加载时间（FCP） | ≤ 2s |
| API 响应时间（非 AI 接口） | ≤ 500ms |
| AI 接口响应时间 | ≤ 30s（含模型生成） |
| 前端路由切换 | ≤ 300ms |
| 数据库查询 | ≤ 100ms（单表） |

### 6.2 安全需求

| 安全项 | 要求 |
|--------|------|
| 密码存储 | Spring Security BCryptPasswordEncoder，强度 ≥ 10 |
| 身份认证 | JWT（jjwt），生产环境密钥 ≥ 32 位随机字符串 |
| API Key 管理 | 迁移到环境变量，不入库代码 |
| 接口鉴权 | 除注册、登录、健康检查外，所有接口需 JWT 鉴权 |
| 输入校验 | 全量接口使用 Bean Validation（Jakarta Validation）校验入参 |
| 限流防刷 | Redis 分布式限流，单 IP 每分钟 ≤ 60 次请求 |
| HTTPS | 上线强制 HTTPS，Let's Encrypt 证书 |
| SQL 注入防护 | MyBatis-Plus 预编译（`#{}`），禁止拼接 SQL |

### 6.3 可用性需求

| 场景 | 要求 |
|------|------|
| 表单操作 | 所有输入框有 placeholder 提示和校验反馈 |
| 加载状态 | 所有异步操作有 loading 动画 |
| 空状态 | 所有列表页有空状态引导文案 |
| 错误提示 | 所有错误有明确的中文提示，不展示技术堆栈 |
| 响应式 | 适配桌面端（≥ 1024px）和移动端（≥ 375px） |

---

## 七、数据模型概述

### 7.1 ER 关系

```
User ──┬── Conversation ── Message              （苏格拉底对话）
       ├── Work                                 （作品工厂）
       ├── ChallengeSubmission ── Challenge     （挑战赛）
       ├── UserBadge ── Badge                   （成就徽章）
       └── PromptSkill                          （能力雷达五维评分）
```

### 7.2 核心表说明

| 表名 | 作用 | 关键字段 |
|------|------|---------|
| users | 用户 | email(唯一)、username(唯一)、passwordHash、streakDays |
| conversations | 对话 | originalPrompt、improvedPrompt、comparisonResult(JSON)、rating、status |
| messages | 消息 | conversationId、role、content、messageType、createdAt |
| prompt_skills | 能力雷达 | userId、accuracy、structure、creativity、constraint、iteration |
| works | 作品 | userId、workType、title、content、shareUrl |
| challenges | 挑战题 | title、description、difficulty、criteria(JSON)、startTime、endTime |
| challenge_submissions | 提交 | challengeId、userId、prompt、score、feedback、rank |
| badges | 徽章定义 | name(唯一)、description、unlockCriteria(JSON) |
| user_badges | 用户徽章 | userId+badgeId 联合唯一、unlockedAt |

### 7.3 索引策略

| 表名 | 索引字段 | 索引类型 | 用途 |
|------|---------|---------|------|
| users | email | UNIQUE | 登录查询 |
| users | username | UNIQUE | 用户名查询 |
| conversations | userId + createdAt | COMPOSITE | 对话历史列表 |
| messages | conversationId | INDEX | 消息列表查询 |
| challenge_submissions | challengeId + score | COMPOSITE | 排行榜查询 |
| works | userId + createdAt | COMPOSITE | 作品列表查询 |

---

## 八、技术架构约束

### 8.1 前端约束

| 约束项 | 说明 |
|--------|------|
| 框架 | React 18.3 + TypeScript 5.8，函数组件 + Hooks |
| 构建 | Vite 6.3，开发端口 5173 |
| 样式 | Tailwind CSS 3.4，不使用其他 CSS 框架 |
| 状态管理 | Zustand 5.0，全局状态通过 store 管理 |
| 路由 | React Router 7.3，统一在 App.tsx 注册 |
| 动画 | Framer Motion 11.15 |
| 图表 | Recharts 2.15（雷达图等） |
| HTTP | Axios 1.7，统一封装 apiClient.ts，拦截器处理 token 和错误 |

### 8.2 后端约束

| 约束项 | 说明 |
|--------|------|
| 框架 | Spring Boot 3.x，Java 21 (LTS) |
| 数据访问 | MyBatis-Plus 3.5，禁止拼接 SQL |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis 7（限流、AI 配额、分布式锁） |
| 认证 | JWT（jjwt），有效期 7 天 |
| 密码 | Spring Security BCryptPasswordEncoder，强度 10 |
| 校验 | Bean Validation（Hibernate Validator），所有接口入参校验 |
| API 文档 | springdoc-openapi（Swagger UI，仅开发/测试环境开启） |
| 监控 | Spring Actuator + Micrometer + Prometheus |
| 安全 | Spring Security，无状态 JWT 鉴权 |
| API 前缀 | /api |
| 端口 | 8080 |

### 8.3 目录结构约定

```
前端 src/（React 18 + Vite，独立部署）
├── components/        # 通用组件（layout/、ui/）
├── pages/             # 页面组件（8 个模块）
├── services/          # API 调用封装
├── store/             # Zustand 状态
├── lib/               # 工具函数
└── App.tsx            # 路由入口

后端 ai-platform/（Java 21 + Spring Boot，功能分块）
├── pom.xml                        # Maven 依赖
├── src/main/java/com/aiplatform/
│   ├── AiPlatformApplication.java # Spring Boot 启动类
│   ├── common/                    # 统一响应 Result、全局异常、工具类
│   ├── config/                    # Redis / Security / MyBatis / AI 配置
│   ├── ai/                        # AI 网关（DeepSeek 封装、熔断重试）
│   └── modules/                   # 功能分块（每模块独立分层）
│       ├── auth/                  #   └─ controller / service / mapper / entity / dto / vo
│       ├── chat/
│       ├── learning/
│       ├── works/
│       ├── templates/
│       ├── lab/
│       ├── compare/
│       ├── badges/
│       ├── profile/
│       └── health/
├── src/main/resources/
│   ├── application.yml            # 数据源、Redis、JWT、AI 配置
│   └── mapper/                    # MyBatis-Plus XML（复杂 SQL）
└── db/
    └── init.sql                   # 建表脚本
```

---

## 九、待开发模块优先级

| 优先级 | 模块 | 关联里程碑 | 预计工期 | 依赖 |
|--------|------|-----------|---------|------|
| P0 | 作品工厂接通后端 | M8 | 2 天 | DeepSeek API |
| P0 | 挑战赛接通后端 | M9 | 3 天 | AI 评分逻辑 |
| P1 | 实验室多模型对比 | M10 | 3 天 | GPT-4o / Claude API |
| P1 | 能力雷达数据真实化 | M11 | 2 天 | 对话 + 挑战赛数据 |
| P1 | 智能对比接通后端 | M12 | 2 天 | DeepSeek API |
| P2 | 模板/徽章持久化 | M13 | 1 天 | 数据库迁移 |
| P0 | 云服务器部署上线 | M14 | 1 天 | M8—M12 完成 |
| P0 | 性能优化 + 安全加固 | M15 | 2 天 | 限流、密钥管理 |

---

## 十、附录

### 10.1 测试账号

| 字段 | 值 |
|------|---|
| 邮箱 | test@example.com |
| 密码 | 123456 |

### 10.2 开发环境

| 环境 | 地址 |
|------|------|
| 前端开发 | http://localhost:5173 |
| 后端 API | http://localhost:8080 |
| 数据库连接 | mysql://root:han1314520@localhost:3306/prompt_learning_platform |
| Redis | localhost:6379 |

### 10.3 关键命令

```bash
# 前端（React + Vite）
npm install
npm run dev               # 前端开发 http://localhost:5173

# 后端（Java + Spring Boot，Maven）
mvn spring-boot:run                 # 后端开发 http://localhost:8080
mvn clean package -DskipTests       # 打包可执行 JAR

# 数据库
mysql -uroot -p < db/init.sql       # 初始化建表

# 部署（Docker 前后端分离）
docker-compose up -d --build        # 启动 前端 Nginx + 后端 + MySQL + Redis
```
