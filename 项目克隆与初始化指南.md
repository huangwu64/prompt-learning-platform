# 项目克隆与初始化指南

> 供后端/前端人员**克隆项目并在本机初始化开发环境**使用。
> 仓库：`https://github.com/huangwu64/prompt-learning-platform`（公开仓库，可直接克隆）
> 更新日期：2026-08-12

---

## 一、前置条件

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| Git | ≥ 2.30 | 配置用户：`git config --global user.name "你的名字"` / `git config --global user.email "你的邮箱"` |
| JDK | 21 (LTS) | 后端运行必需 |
| MySQL | 8.0 | 数据库 |
| Redis | 7.x | 缓存 / 限流 / AI 配额 |
| Node.js | ≥ 18 | 仅前端需要 |

---

## 二、克隆仓库

```bash
git clone https://github.com/huangwu64/prompt-learning-platform.git
cd prompt-learning-platform
```

> 若 HTTPS 不稳定，可用 SSH 方式：`git clone git@github.com:huangwu64/prompt-learning-platform.git`（需先将你的公钥加入 GitHub：Settings → SSH and GPG keys）。

### 2.1 分支说明（务必遵守）

| 分支 | 用途 |
|------|------|
| `master` | 稳定分支，只读 |
| `develop` | **开发基线分支**，日常开发从这里出发 |

```bash
git checkout develop      # 切换到开发基线（必做）
git pull                  # 拉取最新
```

> 开发新功能时从 develop 切功能分支：`git checkout -b feature/backend/xxx` 或 `feature/frontend/xxx`。
> 分支/提交/推拉流程详见 `project/ai-platform/开发协作指南.md`。

---

## 三、后端初始化（ai-platform）

### 3.1 准备数据库

```bash
cd project/ai-platform

# 方式一：Docker 一键起 MySQL + Redis
docker-compose up -d mysql redis

# 方式二：本机已有 MySQL/Redis，手动建库
mysql -uroot -p < db/init.sql
```

### 3.2 配置环境变量

```bash
# 后端必需（生产或 AI 功能必填）
export DEEPSEEK_API_KEY=sk-xxxx          # DeepSeek 密钥
export JWT_SECRET=<至少32位随机字符串>     # JWT 签名密钥

# 可选（默认连本机）
export DB_HOST=localhost
export DB_PORT=3306
export DB_NAME=prompt_learning_platform
export DB_USER=root
export DB_PASSWORD=你的数据库密码
export REDIS_HOST=localhost
export REDIS_PORT=6379
```

### 3.3 启动后端

```bash
./mvnw spring-boot:run          # Windows 用：mvnw.cmd spring-boot:run
```

> `./mvnw` 为项目自带 Maven Wrapper，无需全局安装 Maven，首次运行自动下载依赖。

### 3.4 验证

```bash
curl http://localhost:8080/api/health
# {"success":true,"data":{"status":"ok","services":{"database":"connected","redis":"connected",...}},"error":null}
```

- 后端 API 地址：`http://localhost:8080`
- 接口文档（Swagger，dev 环境）：`http://localhost:8080/swagger-ui.html`
- 完整接口契约：根目录《接口文档.md》

---

## 四、前端初始化（frontend，可选）

前端工程尚未创建，需前端人员按 `project/ai-platform/开发协作指南.md` 第〇章流程初始化：

```bash
cd project
npm create vite@latest frontend -- --template react-ts
cd frontend
npm install
npm run dev                    # http://localhost:5173
```

> Vite 已配置 `/api` 代理到后端 8080，联调无需处理跨域。

---

## 五、开始开发

1. 从 `develop` 切功能分支
2. 后端只改 `project/ai-platform/`，前端只改 `project/frontend/`
3. 提交信息格式：`<type>(<scope>): 描述`（如 `feat(chat): 实现追问接口`）
4. 功能完成合并回 `develop`

---

## 六、常见问题

| 问题 | 解决 |
|------|------|
| `git push` 提示连接重置/超时 | 网络访问 GitHub 不稳定，可重试或配置代理后重试 |
| 后端启动报数据库连不上 | 检查 MySQL 是否启动、`DB_*` 环境变量是否正确 |
| `/api/health` 返回 `database: disconnected` | 确认 `db/init.sql` 已执行、连接串正确 |
| 首次 `./mvnw` 下载很慢 | 需正常网络；或改用国内 Maven 镜像（配置 `~/.m2/settings.xml`） |
