# 项目克隆与开发推送拉取初始化指南

> 本文件指导后端/前端人员**首次克隆项目后，完成 git 开发工作流初始化**（Git 身份、远程、分支、推送拉取）。
> 仓库：`https://github.com/huangwu64/prompt-learning-platform`（公开仓库，可直接克隆）
> 更新日期：2026-08-12

---

## 一、前置：配置 Git 身份（首次必做）

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

---

## 二、克隆仓库

```bash
git clone https://github.com/huangwu64/prompt-learning-platform.git
cd prompt-learning-platform
```

> HTTPS 不稳定时可用 SSH 方式：`git clone git@github.com:huangwu64/prompt-learning-platform.git`（需先将你的公钥加入 GitHub：Settings → SSH and GPG keys）。

---

## 三、确认远程与分支

```bash
git remote -v        # 应看到 origin（HTTPS 或 SSH 地址）
git branch -a        # 应看到 master / develop / origin/...
```

### 3.1 分支基线（必须遵守）

| 分支 | 用途 |
|------|------|
| `master` | 稳定分支，只读，不直接提交 |
| `develop` | **开发基线分支**，日常开发从这里出发（GitHub 默认分支） |

```bash
git checkout develop      # 切到开发基线（克隆后默认即 develop，可跳过）
git pull                  # 拉取最新
```

---

## 四、开发推送拉取初始化流程

### 4.1 开始一个新功能

```bash
git checkout develop
git pull
git checkout -b feature/backend/<功能>     # 后端示例：feature/backend/chat
# 前端示例：git checkout -b feature/frontend/login
```

### 4.2 日常提交

```bash
git add <你的文件>          # 只 add 自己负责目录下的文件
git commit -m "<type>(<scope>): 描述"
```

**提交信息格式**：`<type>(<scope>): <描述>`，例如 `feat(chat): 实现对话追问接口`。

| type | 含义 | 示例 |
|------|------|------|
| `feat` | 新功能 | `feat(chat): 实现对话追问接口` |
| `fix` | 修复 | `fix(auth): 修复 token 过期未跳转` |
| `docs` | 文档 | `docs: 补充接口说明` |
| `refactor` | 重构 | `refactor(works): 抽取模板方法` |
| `test` | 测试 | `test(auth): 增加登录用例` |
| `chore` | 构建/工具 | `chore: 升级依赖版本` |

### 4.3 推送到远程

```bash
git push -u origin feature/backend/chat    # 首次推送（-u 建立跟踪）
git push                                    # 之后增量推送
```

### 4.4 功能完成，合并回 develop

```bash
git checkout develop
git pull
git merge feature/backend/chat
git push
git branch -d feature/backend/chat                    # 删除本地功能分支
git push origin --delete feature/backend/chat         # 删除远程功能分支
```

### 4.5 拉取他人更新

```bash
git pull             # 拉取当前分支的最新更新
git fetch --all      # 获取所有远程分支（含他人新建的功能分支）
```

---

## 五、冲突处理

合并/拉取出现冲突时：

```bash
git status            # 查看冲突文件
# 打开冲突文件，保留正确内容（<<<<<<< HEAD ... ======= ... >>>>>>>）
git add <已解决的文件>
git commit -m "fix: 解决合并冲突"
git push
```

**红线**：
- 禁止 `git push -f`（force push）覆盖他人提交。
- 不确定时先 `git stash` 暂存，不要硬删。
- 冲突解决不了时保留双方内容，找同事确认。

---

## 六、项目环境初始化（简要指引）

> 本文件只讲 **git 开发工作流**。项目本身的启动与初始化另见：

| 事项 | 文档位置 |
|------|---------|
| 后端启动（数据库/环境变量/运行） | `project/ai-platform/README.md` |
| 协作规范全文（分支/提交/冲突/检查清单） | `project/ai-platform/开发协作指南.md` |
| 前端初始化流程 | `project/ai-platform/开发协作指南.md` 第〇章 |
